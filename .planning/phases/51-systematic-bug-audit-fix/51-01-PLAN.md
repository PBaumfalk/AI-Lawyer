---
phase: 51-systematic-bug-audit-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/admin/rollen/page.tsx
  - src/components/akten/falldaten-tab.tsx
  - src/app/api/gamification/special-quests/route.ts
  - src/lib/gamification/condition-templates.ts
  - src/lib/health/checks.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "React hooks are called unconditionally (no hooks after early returns)"
    - "TypeScript compilation passes without errors on falldaten-tab.tsx"
    - "special-quests route.ts only exports GET/POST route handlers"
    - "Stirling PDF health check uses correct port 8081"
  artifacts:
    - path: "src/app/(dashboard)/admin/rollen/page.tsx"
      provides: "Fixed hooks order"
      contains: "useCallback"
    - path: "src/components/akten/falldaten-tab.tsx"
      provides: "Fixed TemplateField type alignment"
      contains: "FalldatenFeldTypDB"
    - path: "src/lib/gamification/condition-templates.ts"
      provides: "Extracted CONDITION_TEMPLATES"
      exports: ["CONDITION_TEMPLATES"]
    - path: "src/app/api/gamification/special-quests/route.ts"
      provides: "Clean route file"
      exports: ["GET", "POST"]
    - path: "src/lib/health/checks.ts"
      provides: "Correct Stirling PDF port"
      contains: "8081"
  key_links:
    - from: "src/app/api/gamification/special-quests/route.ts"
      to: "src/lib/gamification/condition-templates.ts"
      via: "import"
      pattern: "import.*condition-templates"
    - from: "src/components/akten/falldaten-tab.tsx"
      to: "src/components/akten/falldaten-form.tsx"
      via: "TemplateField type compatibility"
      pattern: "FalldatenFeldTypDB"
---

<objective>
Fix all P0 critical bugs: React hooks violation, TypeScript type mismatches, non-route export in API file, and wrong health check port.

Purpose: These are the highest-priority issues — a React hooks violation can crash the page, TypeScript errors are masked by build config, a non-route export can cause Next.js runtime issues, and the wrong port makes Stirling PDF always appear unhealthy.

Output: 5 files fixed, all P0 issues resolved.
</objective>

<execution_context>
@/Users/patrickbaumfalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickbaumfalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-systematic-bug-audit-fix/51-CONTEXT.md

@src/app/(dashboard)/admin/rollen/page.tsx
@src/components/akten/falldaten-tab.tsx
@src/components/akten/falldaten-form.tsx
@src/app/api/gamification/special-quests/route.ts
@src/lib/health/checks.ts

<interfaces>
<!-- Key types the executor needs to understand -->

From src/components/akten/falldaten-form.tsx (the CONSUMER of TemplateField):
```typescript
import type { FalldatenFeldTypDB } from "@/lib/falldaten/validation";

interface TemplateField {
  key: string;
  label: string;
  typ: FalldatenFeldTypDB;  // NOT string — this is the target type
  placeholder?: string | null;
  optionen?: { value: string; label: string }[] | null;
  required?: boolean;
  gruppe?: string | null;
}

interface TemplateSchema {
  label?: string;
  beschreibung?: string;  // string | undefined, NOT string | null
  felder: TemplateField[];
}
```

From src/lib/falldaten/validation.ts:
```typescript
export type FalldatenFeldTypDB = (typeof FALLDATEN_FELD_TYPEN)[number];
// = "text" | "number" | "date" | "boolean" | "select" | "multiselect" | "textarea" | "currency" | "email" | "phone" | "url"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix React hooks violation and type mismatches</name>
  <files>src/app/(dashboard)/admin/rollen/page.tsx, src/components/akten/falldaten-tab.tsx</files>
  <action>
**rollen/page.tsx (line 82):** The `useCallback` for `handleToggleFinanzen` is called after early returns at lines 59-73. Move ALL hooks (useEffect, useCallback) before any conditional returns. Specifically:
- The `useCallback` at line 82 must be moved above the `if (loading)` check at line 59
- The function body can remain the same — just reorder so hooks come first, then early returns, then render

**falldaten-tab.tsx (lines 21-28 and 309):** The local `TemplateField` interface has `typ: string` but `FalldatenForm` expects `typ: FalldatenFeldTypDB`. Also `TemplateData.beschreibung` is `string | null` but `TemplateSchema.beschreibung` expects `string | undefined`.

Fix by:
1. Import `FalldatenFeldTypDB` from `@/lib/falldaten/validation`
2. Change `TemplateField.typ` from `string` to `FalldatenFeldTypDB`
3. On line 309 where `template.beschreibung` (string | null) is passed to `FalldatenForm` schema prop, convert null to undefined: `beschreibung: template.beschreibung ?? undefined`
  </action>
  <verify>
    <automated>cd /Users/patrickbaumfalk/Projekte/AI-Lawyer && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
- useCallback in rollen/page.tsx appears before all early returns
- falldaten-tab.tsx TemplateField.typ is FalldatenFeldTypDB
- beschreibung null-to-undefined conversion in place
- No TypeScript errors in these files
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract non-route export and fix health check port</name>
  <files>src/app/api/gamification/special-quests/route.ts, src/lib/gamification/condition-templates.ts, src/lib/health/checks.ts</files>
  <action>
**special-quests/route.ts:** Next.js route files must only export route handlers (GET, POST, PUT, DELETE, etc.). The file exports `CONDITION_TEMPLATES` at line 29 which is not a route handler.

1. Create new file `src/lib/gamification/condition-templates.ts`
2. Move the `ConditionTemplate` interface and `CONDITION_TEMPLATES` array from route.ts to this new file
3. Update route.ts to import from the new file: `import { CONDITION_TEMPLATES } from "@/lib/gamification/condition-templates"`
4. Verify no other files import CONDITION_TEMPLATES from the route file (grep first)
5. Remove the `export` keyword from CONDITION_TEMPLATES in route.ts (it should only be imported, not re-exported)

**health/checks.ts (line 48):** The Stirling PDF fallback URL defaults to `http://localhost:8090` but docker-compose maps `8081:8080` and .env has `http://localhost:8081`.

Change line 48 from:
```typescript
const url = process.env.STIRLING_PDF_URL || "http://localhost:8090";
```
to:
```typescript
const url = process.env.STIRLING_PDF_URL || "http://localhost:8081";
```
  </action>
  <verify>
    <automated>cd /Users/patrickbaumfalk/Projekte/AI-Lawyer && node -e "const m = require('fs').readFileSync('src/app/api/gamification/special-quests/route.ts','utf8'); const exports = m.match(/^export\s+(const|let|var|function|class|enum|interface|type)\s+/gm) || []; const bad = exports.filter(e => !e.match(/export\s+(async\s+)?function\s+(GET|POST|PUT|DELETE|PATCH|HEAD|OPTIONS)/)); if(bad.length>0){console.error('Non-route exports found:',bad);process.exit(1)}else{console.log('OK: only route handler exports')}" && grep -q "8081" src/lib/health/checks.ts && echo "Port fix OK"</automated>
  </verify>
  <done>
- CONDITION_TEMPLATES extracted to src/lib/gamification/condition-templates.ts
- route.ts imports from the new file, does not re-export non-handler symbols
- Stirling PDF health check uses port 8081
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` shows no errors for falldaten-tab.tsx or special-quests/route.ts
- `npx next lint` no longer shows hooks violation
- Stirling PDF health check default port is 8081
- CONDITION_TEMPLATES lives in src/lib/gamification/condition-templates.ts
</verification>

<success_criteria>
- Zero TypeScript compilation errors (was 3 unique errors / 6 lines)
- React hooks rule of hooks satisfied in rollen/page.tsx
- No non-route exports in any route.ts file for special-quests
- Stirling PDF health check port matches docker-compose mapping (8081)
</success_criteria>

<output>
After completion, create `.planning/phases/51-systematic-bug-audit-fix/51-01-SUMMARY.md`
</output>
