---
phase: 57-helena-intelligence
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/ki/page.tsx
  - src/app/api/akten/[id]/template-suggestions/route.ts
  - src/app/(dashboard)/akten/neu/page.tsx
autonomous: true
requirements: [HEL-05, HEL-06, HEL-07]
must_haves:
  truths:
    - "User can open a global KI-Chat at /ki and ask cross-Akte questions"
    - "Global KI-Chat retrieves context from multiple Akten via RAG"
    - "When creating a new Akte, Helena suggests matching Falldatenblatt templates based on Sachgebiet"
  artifacts:
    - path: "src/app/(dashboard)/ki/page.tsx"
      provides: "Global KI-Chat page with cross-Akte RAG conversation"
      exports: ["default"]
    - path: "src/app/api/akten/[id]/template-suggestions/route.ts"
      provides: "GET endpoint returning template suggestions for Akte based on Sachgebiet"
      exports: ["GET"]
    - path: "src/app/(dashboard)/akten/neu/page.tsx"
      provides: "Updated Akte creation form with template suggestion UI"
  key_links:
    - from: "src/app/(dashboard)/ki/page.tsx"
      to: "/api/ki-chat"
      via: "useChat hook or fetch POST with crossAkte=true"
      pattern: "crossAkte.*true|ki-chat"
    - from: "src/app/(dashboard)/akten/neu/page.tsx"
      to: "/api/falldaten-templates"
      via: "fetch GET with sachgebiet filter on Sachgebiet change"
      pattern: "falldaten-templates.*sachgebiet"
---

<objective>
Implement the global KI-Chat page at /ki for cross-Akte questions and add Falldatenblatt template suggestions when creating a new Akte.

Purpose: Lawyers need to ask questions spanning multiple cases (e.g. "Welche Arbeitsrecht-Akten haben eine Kuendigungsfrist unter 3 Wochen?"). The existing /api/ki-chat already supports crossAkte=true -- this plan creates a dedicated full-page UI. Additionally, suggesting templates at Akte creation saves setup time.

Output: Global chat page, template suggestion API, and enhanced Akte creation form.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/api/ki-chat/route.ts
@src/app/(dashboard)/akten/neu/page.tsx
@src/lib/helena/tools/_read/search-alle-akten.ts

<interfaces>
<!-- Key types and API contracts -->

From /api/ki-chat (existing endpoint):
```
POST /api/ki-chat
Body: { messages: CoreMessage[], akteId?: string | null, conversationId?: string | null, crossAkte?: boolean }
Response: Data stream (Vercel AI SDK streamText response)
Headers: X-Sources (JSON encoded source citations)
```

From /api/ki-chat/conversations (existing):
```
GET /api/ki-chat/conversations — returns user's conversations
POST /api/ki-chat/conversations — creates new conversation
```

From /api/falldaten-templates (existing):
```
GET /api/falldaten-templates?sachgebiet=ARBEITSRECHT&status=STANDARD
Response: { templates: TemplateData[] }
```

From search_alle_akten tool (already registered in Helena tools):
```
Searches across all accessible Akten by Kurzrubrum and Aktenzeichen
Parameters: query, status?, sachgebiet?, limit?
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Global KI-Chat page at /ki</name>
  <files>src/app/(dashboard)/ki/page.tsx</files>
  <action>
Create `src/app/(dashboard)/ki/page.tsx`:

1. "use client" page component. This is a FULL-PAGE chat interface (not a sidebar widget).

2. Use the Vercel AI SDK `useChat` hook from "ai/react":
   ```typescript
   const { messages, input, handleInputChange, handleSubmit, isLoading, setMessages } = useChat({
     api: "/api/ki-chat",
     body: { crossAkte: true, akteId: null },
   });
   ```
   The existing /api/ki-chat already handles crossAkte=true and uses the search_alle_akten tool via RAG pipeline. No backend changes needed for HEL-05/HEL-06.

3. Layout structure:
   - Full height page (h-[calc(100vh-4rem)] to account for dashboard header)
   - Left sidebar (w-64, hidden on mobile): Conversation history
     - "Neues Gespraech" button at top
     - List of past conversations from GET /api/ki-chat/conversations (fetch on mount)
     - Each shows titel, truncated, clickable to load messages
     - Active conversation highlighted
   - Main area: Chat messages + input
     - Messages list with scroll (flex-1 overflow-y-auto)
     - Each message: user messages right-aligned (blue bubble), assistant messages left-aligned (neutral bubble)
     - Assistant messages: parse markdown with basic formatting (bold, lists, code blocks). Use a simple approach: split by \n, handle **bold**, `code`, and - list items. Or use the existing pattern if there is one in the codebase -- check if react-markdown or similar is already a dependency. If not, do basic line-by-line formatting.
     - Strip `<think>...</think>` tags from displayed assistant messages (Helena's internal reasoning should not be shown to user)
     - Show source citations if X-Sources header is present (display below message as collapsible "Quellen" section)
     - Loading indicator: three-dot animation when isLoading
   - Bottom: Input bar (fixed at bottom of chat area)
     - Textarea with auto-resize (max 4 rows)
     - Send button (ArrowUp icon) enabled when input is non-empty and not loading
     - Submit on Enter (not Shift+Enter), Shift+Enter for newline

4. Conversation management:
   - On new message completion, save to conversation via the conversationId flow (already handled in /api/ki-chat onFinish)
   - Pass conversationId in body once a conversation is established
   - "Neues Gespraech" clears messages and resets conversationId

5. Page header: "Helena KI-Chat" with Bot icon (lucide-react), subtitle "Aktenuebergreifende Recherche"

6. Glassmorphism styling matching dashboard patterns (bg-white/50 dark:bg-white/[0.05] backdrop-blur-md for cards/bubbles, border-white/20)

7. Mobile: sidebar hidden, show hamburger to toggle. Chat area full width.

8. Add navigation entry: Check if there's a sidebar nav component. If so, add /ki link with Bot icon. Search for the dashboard sidebar/nav component and add the entry. Look for patterns like navItems or menuItems arrays.
  </action>
  <verify>
    <automated>cd /Users/jarvis/Projekte/AI-Lawyer && npx tsc --noEmit src/app/\\(dashboard\\)/ki/page.tsx 2>&1 | head -30</automated>
  </verify>
  <done>/ki page renders a full-page chat interface with conversation sidebar, message bubbles, source citations, and cross-Akte RAG via existing /api/ki-chat with crossAkte=true. Navigation entry added.</done>
</task>

<task type="auto">
  <name>Task 2: Template suggestions at Akte creation</name>
  <files>src/app/api/akten/[id]/template-suggestions/route.ts, src/app/(dashboard)/akten/neu/page.tsx</files>
  <action>
Create `src/app/api/akten/[id]/template-suggestions/route.ts`:
- Actually, this should be a non-Akte-specific endpoint since the Akte doesn't exist yet at creation time.
- Instead, use the EXISTING endpoint: GET /api/falldaten-templates?sachgebiet=ARBEITSRECHT&status=STANDARD already returns matching templates. No new backend endpoint needed for HEL-07.

Modify `src/app/(dashboard)/akten/neu/page.tsx`:

1. Add state: `suggestedTemplates: TemplateData[]` (array of {id, name, beschreibung, sachgebiet, status}), `selectedTemplateId: string | null`, `loadingTemplates: boolean`

2. When the user selects a Sachgebiet (onChange of the sachgebiet Select):
   - Fetch GET `/api/falldaten-templates?sachgebiet=${selectedSachgebiet}`
   - Filter results for status STANDARD or GENEHMIGT
   - Set suggestedTemplates state

3. Display template suggestions below the Sachgebiet selector:
   - If suggestedTemplates.length > 0:
     - Show a small section "Helena empfiehlt:" with Sparkles icon
     - List each template as a selectable card/radio button:
       - Template name (bold)
       - Template beschreibung (muted, if available)
       - Status badge (STANDARD in green, GENEHMIGT in blue)
     - Pre-select the STANDARD template if one exists
     - Selected template highlighted with primary border
   - If loading: show small spinner
   - If no templates: show nothing (no empty state needed)

4. When the Akte creation form submits:
   - Include `falldatenTemplateId: selectedTemplateId` in the POST body to /api/akten
   - The existing POST /api/akten endpoint already accepts falldatenTemplateId (check: the Akte model has falldatenTemplateId field)

5. Styling: match the existing form layout. Template suggestions should be unobtrusive -- a small section that appears after Sachgebiet selection, not a full dialog.

6. If Sachgebiet is "SONSTIGES", skip template fetch (no templates for SONSTIGES).
  </action>
  <verify>
    <automated>cd /Users/jarvis/Projekte/AI-Lawyer && npx tsc --noEmit src/app/\\(dashboard\\)/akten/neu/page.tsx 2>&1 | head -30</automated>
  </verify>
  <done>Selecting a Sachgebiet during Akte creation shows matching Falldatenblatt template suggestions. User can select a template that gets assigned on creation. SONSTIGES shows no suggestions.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `npx tsc --noEmit` passes for all modified files
2. /ki page exists: verify page.tsx at correct path
3. crossAkte flag: grep for "crossAkte" in ki/page.tsx confirms true
4. Template suggestions: grep for "falldaten-templates" fetch in akten/neu/page.tsx
5. Navigation: grep for "/ki" in sidebar/nav component
</verification>

<success_criteria>
- /ki page renders full-page chat with conversation history sidebar
- Cross-Akte questions retrieve context from multiple Akten via existing RAG pipeline
- Akte creation form suggests Falldatenblatt templates when Sachgebiet is selected
- Selected template is assigned to the new Akte on creation
- All TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/57-helena-intelligence/57-03-SUMMARY.md`
</output>
