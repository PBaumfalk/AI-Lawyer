---
phase: 55-bi-dashboard-export
plan: 03
type: execute
wave: 2
depends_on: ["55-01"]
files_modified:
  - src/app/(dashboard)/bi/page.tsx
  - src/app/(dashboard)/bi/layout.tsx
  - src/components/bi/kpi-grid.tsx
  - src/components/bi/bi-filters.tsx
  - src/components/bi/trend-charts.tsx
  - src/hooks/use-bi-data.ts
autonomous: true
requirements:
  - BI-01
  - BI-02
  - BI-03
  - BI-04
  - BI-05
  - BI-06
  - BI-07
  - BI-08

must_haves:
  truths:
    - "User can open /bi page and see KPI tiles for Akten, Finanzen, Fristen, Helena"
    - "Each KPI tile shows month-over-month delta with arrow and percentage"
    - "User can filter by Zeitraum (Monat/Quartal/Jahr/Custom), Anwalt, and Sachgebiet"
    - "Filters update KPIs and charts immediately without page reload"
    - "User can view Akten-Neuzugang, Umsatz pro Monat, Fristen-Compliance as line/area charts"
    - "Fristen-Compliance rate displayed as percentage"
    - "Helena adoption metrics visible (Gespraeche, Entwuerfe, Akzeptanzrate, Token-Verbrauch)"
  artifacts:
    - path: "src/app/(dashboard)/bi/page.tsx"
      provides: "BI Dashboard page"
      min_lines: 30
    - path: "src/components/bi/kpi-grid.tsx"
      provides: "KPI tile grid with delta indicators"
    - path: "src/components/bi/bi-filters.tsx"
      provides: "Filter bar with Zeitraum, Anwalt, Sachgebiet selectors"
    - path: "src/components/bi/trend-charts.tsx"
      provides: "Recharts line/area charts for trends"
    - path: "src/hooks/use-bi-data.ts"
      provides: "SWR hook for BI data fetching with filter params"
  key_links:
    - from: "src/hooks/use-bi-data.ts"
      to: "/api/bi/kpis"
      via: "SWR fetch with filter query params"
      pattern: "fetch.*api/bi/kpis"
    - from: "src/hooks/use-bi-data.ts"
      to: "/api/bi/trends"
      via: "SWR fetch with filter query params"
      pattern: "fetch.*api/bi/trends"
    - from: "src/components/bi/trend-charts.tsx"
      to: "recharts"
      via: "LineChart, AreaChart, ResponsiveContainer imports"
      pattern: "from.*recharts"
---

<objective>
Build the BI Dashboard UI page at /bi with KPI tiles (including deltas), filter bar, and Recharts trend visualizations consuming the API from Plan 01.

Purpose: Provides the analytics view fulfilling BI-01 through BI-08 (UI side).
Output: Interactive BI Dashboard page with real-time filtering and chart visualizations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/55-bi-dashboard-export/55-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that this plan consumes -->

From src/lib/bi/types.ts:
```typescript
export interface BiFilterParams {
  zeitraum: "monat" | "quartal" | "jahr" | "custom";
  von?: string;
  bis?: string;
  anwaltId?: string;
  sachgebiet?: string;
}

export interface KpiTile {
  id: string;
  label: string;
  value: number;
  previousValue: number;
  delta: number;
  unit?: string;
  domain: "akten" | "finanzen" | "fristen" | "helena";
}

export interface BiKpiResponse {
  tiles: KpiTile[];
  zeitraum: { von: string; bis: string };
  cached: boolean;
}

export interface TrendSeries {
  id: string;
  label: string;
  data: TrendPoint[];
  type: "line" | "area";
}

export interface BiTrendResponse {
  series: TrendSeries[];
  zeitraum: { von: string; bis: string };
  cached: boolean;
}
```
</interfaces>

@src/components/ui/glass-kpi-card.tsx
@src/components/ui/glass-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BI data hook and filter component</name>
  <files>src/hooks/use-bi-data.ts, src/components/bi/bi-filters.tsx</files>
  <action>
Create `src/hooks/use-bi-data.ts`:
- "use client" is not needed for hooks, but ensure it works in client components
- Use SWR (already in project) or plain useState + useEffect with fetch
- Check if swr is in package.json first. If yes, use useSWR. If not, use useState + useEffect + useCallback pattern.
- Export `useBiKpis(filters: BiFilterParams)` -- fetches GET /api/bi/kpis with filter query params, returns `{ kpis: KpiTile[], loading: boolean, error: string | null }`
- Export `useBiTrends(filters: BiFilterParams)` -- fetches GET /api/bi/trends, returns `{ series: TrendSeries[], loading: boolean, error: string | null }`
- Build query string from BiFilterParams (skip undefined values)
- Re-fetch when filters change
- Import types from `@/lib/bi/types`

Create `src/components/bi/bi-filters.tsx`:
- "use client" component
- Props: `{ filters: BiFilterParams; onChange: (filters: BiFilterParams) => void }`
- Render a horizontal filter bar inside a GlassCard:
  1. **Zeitraum**: Select/dropdown with options: Monat, Quartal, Jahr, Custom. When "Custom" selected, show two date inputs (von/bis) using the existing date picker pattern from the project
  2. **Anwalt**: Select dropdown populated by fetching GET /api/admin/users (or similar endpoint that returns users with ANWALT/PARTNER role). "Alle" as default option.
  3. **Sachgebiet**: Select dropdown with all Sachgebiet enum values from the schema (ARBEITSRECHT, FAMILIENRECHT, etc.). "Alle" as default.
- On any change, call onChange with updated filters
- Use shadcn Select component pattern
- Responsive: stack vertically on mobile, horizontal on desktop (flex-wrap)
  </action>
  <verify>npx tsc --noEmit src/hooks/use-bi-data.ts src/components/bi/bi-filters.tsx 2>&1 | head -20</verify>
  <done>useBiKpis and useBiTrends hooks fetch from BI API endpoints with filter params. BiFilters component renders Zeitraum/Anwalt/Sachgebiet selectors and calls onChange on filter change.</done>
</task>

<task type="auto">
  <name>Task 2: Build KPI grid, trend charts, and BI Dashboard page</name>
  <files>src/components/bi/kpi-grid.tsx, src/components/bi/trend-charts.tsx, src/app/(dashboard)/bi/page.tsx, src/app/(dashboard)/bi/layout.tsx</files>
  <action>
Create `src/components/bi/kpi-grid.tsx`:
- "use client" component
- Props: `{ tiles: KpiTile[]; loading: boolean }`
- Render a 4-column grid (responsive: 1 col mobile, 2 md, 4 lg) using GlassKpiCard
- For each tile: display label, value (formatted: EUR with Intl.NumberFormat for currency, % for percentages, plain number for counts), icon based on domain
- **Delta indicator**: Below the value, show an arrow (ArrowUpRight or ArrowDownRight from lucide-react) with the delta percentage. Green for positive, red for negative, gray for zero. Format: "+12.5%" or "-3.2%"
- When loading=true, render 8 skeleton GlassKpiCards (the component supports skeleton prop)
- Group tiles by domain with section labels: "Akten", "Finanzen", "Fristen", "Helena"

Create `src/components/bi/trend-charts.tsx`:
- "use client" component
- Props: `{ series: TrendSeries[]; loading: boolean }`
- Import from recharts: ResponsiveContainer, LineChart, AreaChart, Line, Area, XAxis, YAxis, CartesianGrid, Tooltip, Legend
- Render each series in a GlassCard:
  - If series.type === "line": use LineChart
  - If series.type === "area": use AreaChart with gradient fill
- XAxis: data[].label (month labels)
- YAxis: auto scale
- Use Tailwind-compatible colors (blue-500, emerald-500, amber-500)
- Tooltip: show value with proper formatting (EUR for Umsatz, % for Compliance, count for Neuzugang)
- Responsive: full width, height 300px
- When loading: show skeleton placeholder cards

Create `src/app/(dashboard)/bi/layout.tsx`:
- Simple layout wrapper with metadata title "BI-Dashboard"

Create `src/app/(dashboard)/bi/page.tsx`:
- "use client" page
- State: `filters` (BiFilterParams, default zeitraum="monat")
- Use useBiKpis(filters) and useBiTrends(filters)
- Layout:
  1. Page header: "BI-Dashboard" title with subtitle "Kanzlei-Analytics im Ueberblick"
  2. BiFilters component
  3. KpiGrid with tiles from useBiKpis
  4. TrendCharts with series from useBiTrends
- Add navigation link to sidebar: check existing sidebar component (likely in src/components/layout/) and add "BI-Dashboard" link with BarChart3 icon from lucide-react, pointing to /bi. Place it in the analytics/admin section of the nav.
  </action>
  <verify>npx tsc --noEmit src/components/bi/kpi-grid.tsx src/components/bi/trend-charts.tsx src/app/\(dashboard\)/bi/page.tsx 2>&1 | head -20</verify>
  <done>BI Dashboard page at /bi renders KPI grid with delta arrows, filter bar with Zeitraum/Anwalt/Sachgebiet, and Recharts trend charts. Sidebar includes BI-Dashboard link. All data updates immediately when filters change.</done>
</task>

</tasks>

<verification>
- /bi page loads and shows KPI tiles grouped by domain
- Each tile shows delta with colored arrow and percentage
- Filter dropdowns work: changing Zeitraum/Anwalt/Sachgebiet re-fetches data
- Trend charts render as line/area visualizations with proper axes and tooltips
- Page is responsive (mobile: stacked, desktop: grid)
- Sidebar navigation includes BI-Dashboard link
</verification>

<success_criteria>
User can navigate to /bi, see KPI tiles for Akten/Finanzen/Fristen/Helena with month-over-month deltas, filter by Zeitraum/Anwalt/Sachgebiet with immediate updates, and view trend charts as Recharts line/area visualizations.
</success_criteria>

<output>
After completion, create `.planning/phases/55-bi-dashboard-export/55-03-SUMMARY.md`
</output>
