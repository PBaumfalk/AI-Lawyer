---
phase: 62-composer-telefonnotiz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/manual/add_telefonnotiz_type.sql
  - src/app/api/akten/[id]/feed/route.ts
  - src/components/akten/activity-feed-composer.tsx
  - src/components/akten/activity-feed-entry.tsx
  - src/components/akten/activity-feed.tsx
autonomous: true
requirements: [FEED-04, FEED-05]

must_haves:
  truths:
    - "Composer shows a type selector with Notiz, Telefonnotiz, and Aufgabe options"
    - "Selecting Telefonnotiz opens an overlay form with Beteiligter, Ergebnis, Stichworte, naechster Schritt fields"
    - "Submitting Telefonnotiz creates an AktenActivity with typ TELEFONNOTIZ and meta containing all fields"
    - "New Telefonnotiz entry appears immediately in the feed without page reload"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "TELEFONNOTIZ value in AktenActivityTyp enum"
      contains: "TELEFONNOTIZ"
    - path: "src/app/api/akten/[id]/feed/route.ts"
      provides: "POST handler accepting typ=TELEFONNOTIZ with meta payload"
      exports: ["GET", "POST"]
    - path: "src/components/akten/activity-feed-composer.tsx"
      provides: "Type selector tabs and Telefonnotiz overlay form"
      min_lines: 100
    - path: "src/components/akten/activity-feed-entry.tsx"
      provides: "Rendering for TELEFONNOTIZ feed entries with meta display"
      contains: "TELEFONNOTIZ"
  key_links:
    - from: "src/components/akten/activity-feed-composer.tsx"
      to: "/api/akten/[id]/feed"
      via: "fetch POST with typ and meta fields"
      pattern: "typ.*TELEFONNOTIZ"
    - from: "src/app/api/akten/[id]/feed/route.ts"
      to: "prisma.aktenActivity.create"
      via: "Prisma create with TELEFONNOTIZ typ"
      pattern: "TELEFONNOTIZ"
    - from: "src/components/akten/activity-feed-entry.tsx"
      to: "FeedEntryData.meta"
      via: "Renders beteiligter, ergebnis, stichworte, naechsterSchritt from meta"
      pattern: "meta\\.beteiligter|meta\\.ergebnis"
---

<objective>
Add TELEFONNOTIZ enum value, extend the feed composer with a type selector (Notiz / Telefonnotiz / Aufgabe tabs), and implement the Telefonnotiz overlay form that captures Beteiligter, Ergebnis, Stichworte, and naechster Schritt. On submit, the entry appears immediately in the feed.

Purpose: Users need structured phone note capture directly from the activity feed -- not just plain text notes.
Output: Extended composer with type selector, Telefonnotiz form, TELEFONNOTIZ entries in feed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-feed-cleanup/61-01-SUMMARY.md

@src/components/akten/activity-feed-composer.tsx
@src/components/akten/activity-feed.tsx
@src/components/akten/activity-feed-entry.tsx
@src/app/api/akten/[id]/feed/route.ts
@prisma/schema.prisma

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/components/akten/activity-feed-entry.tsx:
```typescript
export interface FeedEntryData {
  id: string;
  akteId: string;
  typ: string; // AktenActivityTyp
  titel: string;
  inhalt: string | null;
  meta: Record<string, any> | null;
  createdAt: string;
  user: { id: string; name: string } | null;
}
```

From src/components/akten/activity-feed.tsx (composer integration):
```typescript
<ActivityFeedComposer
  akteId={akteId}
  onNoteCreated={(entry) => {
    setEntries((prev) => [entry, ...prev]);
  }}
  onHelenaTaskStarted={() => {}}
/>
```

From prisma/schema.prisma:
```prisma
enum AktenActivityTyp {
  DOKUMENT
  FRIST
  EMAIL
  HELENA_DRAFT
  HELENA_ALERT
  NOTIZ
  BETEILIGTE
  STATUS_CHANGE
}
```

From src/app/api/akten/[id]/beteiligte/route.ts:
GET returns beteiligte with kontakt (vorname, nachname, firma) for the Akte.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TELEFONNOTIZ enum + extend API + feed entry rendering</name>
  <files>prisma/schema.prisma, prisma/migrations/manual/add_telefonnotiz_type.sql, src/app/api/akten/[id]/feed/route.ts, src/components/akten/activity-feed-entry.tsx, src/components/akten/activity-feed.tsx</files>
  <action>
1. In prisma/schema.prisma, add `TELEFONNOTIZ` to the `AktenActivityTyp` enum (after NOTIZ). Also add `AUFGABE` while here (needed for plan 02). Run `npx prisma generate` to regenerate the client.

2. Create `prisma/migrations/manual/add_telefonnotiz_type.sql` with:
```sql
ALTER TYPE "AktenActivityTyp" ADD VALUE IF NOT EXISTS 'TELEFONNOTIZ';
ALTER TYPE "AktenActivityTyp" ADD VALUE IF NOT EXISTS 'AUFGABE';
```
Run this migration against the database: `npx prisma db execute --file prisma/migrations/manual/add_telefonnotiz_type.sql`

3. In `src/app/api/akten/[id]/feed/route.ts` POST handler:
   - Accept optional `typ` field from body (default "NOTIZ" for backward compat)
   - Accept optional `meta` object from body
   - When typ is "TELEFONNOTIZ", validate that meta contains `beteiligter` (string, required), `ergebnis` (string, required), `stichworte` (string, optional), `naechsterSchritt` (string, optional)
   - Set titel to `"Telefonnotiz: ${meta.beteiligter}"` for TELEFONNOTIZ entries
   - When typ is "AUFGABE", validate meta contains `titel` (string, required), `faelligAm` (string/date, optional), `beschreibung` (string, optional). Set titel to meta.titel. Store meta with the activity.
   - For plain NOTIZ, keep existing behavior unchanged
   - @Helena mention detection should only apply to NOTIZ typ (not TELEFONNOTIZ or AUFGABE)

4. In `src/components/akten/activity-feed-entry.tsx`:
   - Add TELEFONNOTIZ to the icon mapping (use `Phone` icon from lucide-react)
   - Add AUFGABE to icon mapping (use `CheckSquare` icon from lucide-react)
   - In the entry rendering, when typ is TELEFONNOTIZ, show structured meta display:
     - "Beteiligter: {meta.beteiligter}"
     - "Ergebnis: {meta.ergebnis}"
     - If meta.stichworte: "Stichworte: {meta.stichworte}"
     - If meta.naechsterSchritt: "Naechster Schritt: {meta.naechsterSchritt}"
   - When typ is AUFGABE, show meta.beschreibung and meta.faelligAm if present
   - Add TELEFONNOTIZ and AUFGABE labels to sanitizeTitel (if enum replacement is relevant)

5. In `src/components/akten/activity-feed.tsx`:
   - Add TELEFONNOTIZ to the "Kommunikation" filter chip types array (alongside EMAIL and NOTIZ)
   - Add AUFGABE to a filter -- add it to "Fristen" chip types (alongside FRIST) since tasks/Aufgaben are deadline-related
  </action>
  <verify>
    <automated>cd /Users/jarvis/Projekte/AI-Lawyer && npx prisma validate && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TELEFONNOTIZ and AUFGABE enum values exist in schema, API accepts typ+meta in POST, feed entries render structured Telefonnotiz and Aufgabe data, filter chips include new types</done>
</task>

<task type="auto">
  <name>Task 2: Composer type selector tabs and Telefonnotiz overlay form</name>
  <files>src/components/akten/activity-feed-composer.tsx</files>
  <action>
Rewrite the ActivityFeedComposer to support three composition modes via tab-like selector buttons above the input area:

1. **Type selector row**: Three small pill buttons at the top of the composer: "Notiz" (default, active), "Telefonnotiz", "Aufgabe". Use the same chip styling as the feed filter chips for consistency. Active chip uses brand-600 bg.

2. **Notiz mode** (default): Keep existing behavior exactly -- textarea with @Helena button and Send button. No changes.

3. **Telefonnotiz mode**: Replace the simple textarea with a structured form overlay:
   - `beteiligter` input (text, required, placeholder "Name des Gespraechspartners")
   - `ergebnis` select/dropdown with options: "Erreicht", "Nicht erreicht", "Rueckruf erbeten", "Nachricht hinterlassen", "Besetzt"
   - `stichworte` textarea (optional, placeholder "Stichworte zum Gespraech...")
   - `naechsterSchritt` input (optional, placeholder "Naechster Schritt / Follow-up")
   - Send button submits POST to /api/akten/{akteId}/feed with `{ typ: "TELEFONNOTIZ", text: stichworte, meta: { beteiligter, ergebnis, stichworte, naechsterSchritt } }`
   - On success, call onNoteCreated with the returned activity and reset form
   - Form uses the same glass-card styling, compact layout with 2-column grid for beteiligter+ergebnis, full-width for stichworte and naechsterSchritt
   - Form validation: beteiligter and ergebnis are required, show red border if empty on submit attempt

4. **Aufgabe mode**: Simple form with:
   - `titel` input (required, placeholder "Aufgabe beschreiben...")
   - `faelligAm` date input (optional)
   - `beschreibung` textarea (optional, placeholder "Details...")
   - Submits POST to /api/akten/{akteId}/feed with `{ typ: "AUFGABE", text: titel, meta: { titel, faelligAm, beschreibung } }`
   - On success, call onNoteCreated with returned activity and reset form

5. Keep the existing onHelenaTaskStarted callback -- only trigger it for NOTIZ typ (existing @Helena logic).

6. The composer maintains its sticky position at the bottom of the feed (flex-shrink-0 in parent).
  </action>
  <verify>
    <automated>cd /Users/jarvis/Projekte/AI-Lawyer && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Composer shows Notiz/Telefonnotiz/Aufgabe tabs. Telefonnotiz tab shows structured form with Beteiligter, Ergebnis, Stichworte, naechster Schritt. Aufgabe tab shows titel/faelligAm/beschreibung form. Both submit correctly and entries appear in feed.</done>
</task>

</tasks>

<verification>
1. `npx prisma validate` passes
2. `npx tsc --noEmit` compiles without errors
3. Feed API POST accepts `{ text, typ: "TELEFONNOTIZ", meta: { beteiligter, ergebnis, stichworte, naechsterSchritt } }` and returns 201
4. Feed API POST still works without typ field (backward compat as NOTIZ)
5. Composer renders three mode tabs
6. Telefonnotiz form validates required fields before submit
</verification>

<success_criteria>
- Composer has visible Notiz/Telefonnotiz/Aufgabe type selector
- Telefonnotiz overlay captures Beteiligter, Ergebnis, Stichworte, naechster Schritt
- Submitting Telefonnotiz creates feed entry with structured meta
- Submitting Aufgabe creates feed entry with titel and optional deadline
- New entries appear immediately in the feed (optimistic prepend)
- Existing Notiz + @Helena behavior unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/62-composer-telefonnotiz/62-01-SUMMARY.md`
</output>
