# Continue: discuss-phase 26

**Paused at:** present_gray_areas (AskUserQuestion not capturing selections — likely UI issue)
**Resume with:** `/gsd:discuss-phase 26`

## What's done
- Phase 26 validated against roadmap ✓
- Phase directory created ✓
- Codebase scouted ✓
- Gray areas identified ✓
- AskUserQuestion attempted 3x but selections came back empty

## Codebase scout findings

### Current Akte Detail Layout
- `akte-detail-tabs.tsx` uses shadcn `Tabs` with 11 tabs: Uebersicht, Beteiligte, Falldaten, Dokumente, Kalender, Aktenkonto, Rechnungen, Zeiterfassung, E-Mails, Pruefprotokoll, Warnungen, Historie
- Phase 26 UI-01 wants to replace this tab navigation with a unified chronological Activity Feed

### Existing Helena UI Components
- `alert-center.tsx` — full Alert-Center with filters (Phase 24)
- `draft-card.tsx`, `draft-detail-modal.tsx`, `draft-inbox.tsx` — Draft review UI (Phase 23)
- `draft-pinned-section.tsx` — Drafts pinned in Akte view
- `akte-alerts-section.tsx` — Alerts in Akte Warnungen tab

### AktenActivity Model (Phase 19)
- Prisma model with 8 event types: DOKUMENT, FRIST, EMAIL, HELENA_DRAFT, HELENA_ALERT, NOTIZ, BETEILIGTE, STATUS_CHANGE
- Already created by scanner (HELENA_ALERT), draft-notification (HELENA_DRAFT)

### Integration Issues from Audit
- `at-mention-parser.ts` is orphaned — UI-02 (Composer with @Helena) will wire it
- `helenaTaskId` not propagated to HelenaDraft — low priority tech debt

## Phase 26 Requirements (13 total)
- UI-01: Activity Feed replacing tabs
- UI-02: Composer in feed (@Helena tagging)
- UI-03: Helena vs Human attribution
- UI-04: Alert-Center dashboard widget
- UI-05: Helena task status in chat
- UI-06: Draft review inline in feed
- QA-01: Goldset test catalog (>=20 queries)
- QA-02: Retrieval metrics (Recall@k, MRR)
- QA-03: Hallucination check (§ references verified)
- QA-04: Schriftsatz retrieval log
- QA-05: UI "Helena used X Normen, Y Urteile" with source links
- QA-06: Release gates (Recall>=0.85, Halluzinationsrate<=0.05)
- QA-07: No Mandantendaten in logs

## Gray areas to discuss (4 areas)

### 1. Feed layout + tabs replacement (BIGGEST DECISION)
Key question: How do 11 tabs coexist with the new Activity Feed?
Options presented (user hasn't chosen yet):
- A) Feed + sidebar sections (no tab bar)
- B) Feed as default tab (simplified tab bar: Feed, Dokumente, Kalender, Finanzen)
- C) Feed replaces everything (pure feed, inline expansion)

### 2. Composer + @Helena + inline drafts
- Where does the note composer sit?
- How does @Helena trigger work visually?
- How does inline draft review work?

### 3. QA infrastructure approach
- How do goldset tests run? (CLI vs admin UI)
- Where are retrieval metrics surfaced?
- Hallucination check: batch vs inline?

### 4. Helena attribution + task progress
- Helena vs human visual distinction in feed
- Running task step display (step X/Y)
- Alert-Center dashboard widget

## Next step
Resume discuss-phase — present gray areas again, deep-dive each selected area, write CONTEXT.md.
