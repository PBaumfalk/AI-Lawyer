---
phase: 51-systematic-bug-audit-fix
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/error.tsx
  - src/app/not-found.tsx
  - src/app/(dashboard)/error.tsx
  - src/app/(dashboard)/loading.tsx
  - src/app/(portal)/error.tsx
  - src/app/(portal)/loading.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Unhandled errors in dashboard show a recovery UI instead of blank page"
    - "Unhandled errors in portal show a recovery UI instead of blank page"
    - "Root-level errors are caught by a global error boundary"
    - "Unknown routes show a styled 404 page"
    - "Loading states show a spinner/skeleton during page transitions"
  artifacts:
    - path: "src/app/error.tsx"
      provides: "Root error boundary"
      contains: "use client"
    - path: "src/app/not-found.tsx"
      provides: "Custom 404 page"
      contains: "404"
    - path: "src/app/(dashboard)/error.tsx"
      provides: "Dashboard error boundary"
      contains: "use client"
    - path: "src/app/(dashboard)/loading.tsx"
      provides: "Dashboard loading state"
      contains: "Loader"
    - path: "src/app/(portal)/error.tsx"
      provides: "Portal error boundary"
      contains: "use client"
    - path: "src/app/(portal)/loading.tsx"
      provides: "Portal loading state"
      contains: "Loader"
  key_links:
    - from: "src/app/error.tsx"
      to: "src/app/layout.tsx"
      via: "Next.js error boundary convention"
      pattern: "error\\.tsx"
    - from: "src/app/(dashboard)/error.tsx"
      to: "src/app/(dashboard)/layout.tsx"
      via: "Next.js route group error boundary"
      pattern: "error\\.tsx"
---

<objective>
Add error boundaries, loading states, and a custom 404 page for all route groups.

Purpose: Currently, any unhandled error in a Server Component or page render crashes to a blank/generic page with no way to recover. Users must manually navigate away. Error boundaries catch these and show a "try again" UI. Loading states prevent blank screens during navigation.

Output: 6 new files providing error recovery and loading feedback.
</objective>

<execution_context>
@/Users/patrickbaumfalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickbaumfalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-systematic-bug-audit-fix/51-CONTEXT.md

@src/app/layout.tsx
@src/app/(dashboard)/layout.tsx

<interfaces>
<!-- Design system context for consistent styling -->

The app uses oklch Glass UI with dark mode. Key UI patterns:
- GlassPanel component for card-like containers
- Tailwind CSS with oklch colors
- German language for all user-facing text
- Fonts: Inter / system-ui stack
- Lucide icons

From src/components/ui/glass-panel.tsx:
```typescript
export function GlassPanel({ children, className }: { children: React.ReactNode; className?: string })
```

Common classes: `bg-card/60 backdrop-blur-md border border-border/40 rounded-xl`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add root error boundary and 404 page</name>
  <files>src/app/error.tsx, src/app/not-found.tsx</files>
  <action>
**src/app/error.tsx** — Root-level error boundary. Must be a client component.

Create with:
- `"use client"` directive
- Props: `{ error: Error & { digest?: string }; reset: () => void }`
- Glass UI styling consistent with the app (use Tailwind classes, not GlassPanel — error boundary should have minimal imports to avoid cascading failures)
- German text: "Etwas ist schiefgelaufen" as heading
- Show `error.message` in a muted text block (truncated to 200 chars)
- Show `error.digest` if present (small muted text)
- "Erneut versuchen" button calling `reset()`
- "Zur Startseite" link to `/dashboard`
- useEffect to log `console.error("[ErrorBoundary]", error)` on mount
- Center the content vertically and horizontally
- Use red accent color for the error icon (AlertTriangle from lucide-react)

**src/app/not-found.tsx** — Custom 404 page (can be server component).

Create with:
- German text: "Seite nicht gefunden" heading, "Die angeforderte Seite existiert nicht." description
- "Zum Dashboard" link to `/dashboard`
- "Zur Startseite" link to `/`
- Centered layout with glass styling
- FileQuestion icon from lucide-react
  </action>
  <verify>
    <automated>cd /Users/patrickbaumfalk/Projekte/AI-Lawyer && test -f src/app/error.tsx && test -f src/app/not-found.tsx && grep -q '"use client"' src/app/error.tsx && grep -q "reset" src/app/error.tsx && grep -q "404\|nicht gefunden" src/app/not-found.tsx && echo "PASS: Root error boundary and 404 exist"</automated>
  </verify>
  <done>
- src/app/error.tsx exists with "use client", error display, and reset button
- src/app/not-found.tsx exists with German 404 message and navigation links
  </done>
</task>

<task type="auto">
  <name>Task 2: Add dashboard and portal error boundaries and loading states</name>
  <files>src/app/(dashboard)/error.tsx, src/app/(dashboard)/loading.tsx, src/app/(portal)/error.tsx, src/app/(portal)/loading.tsx</files>
  <action>
**src/app/(dashboard)/error.tsx** — Dashboard-specific error boundary.

Similar to root error.tsx but:
- `"use client"` directive
- "Fehler im Dashboard" heading
- Same error.message display and reset/link pattern
- "Erneut versuchen" button + "Zum Dashboard" link to `/dashboard`
- useEffect to console.error
- Glass UI style: `bg-card/60 backdrop-blur-md border border-border/40 rounded-xl p-8`

**src/app/(dashboard)/loading.tsx** — Dashboard loading state.

- Simple centered spinner with German text "Laden..."
- Use Loader2 icon from lucide-react with `animate-spin`
- Minimal: `<div className="flex items-center justify-center min-h-[50vh]"><Loader2 className="h-8 w-8 animate-spin text-muted-foreground" /><span className="ml-3 text-muted-foreground">Laden...</span></div>`

**src/app/(portal)/error.tsx** — Portal-specific error boundary.

Similar to dashboard error.tsx but:
- "Fehler im Portal" heading
- "Zum Portal" link to `/portal`
- Slightly different styling if needed (portal has its own layout)

**src/app/(portal)/loading.tsx** — Portal loading state.

Same pattern as dashboard loading.tsx. Simple centered Loader2 spinner with "Laden..." text.

**All error boundaries must:**
- Be client components (`"use client"`)
- Accept `{ error: Error & { digest?: string }; reset: () => void }`
- Call `console.error` in a useEffect
- Have a "try again" button calling `reset()`
- Have a navigation link back to the group's root
  </action>
  <verify>
    <automated>cd /Users/patrickbaumfalk/Projekte/AI-Lawyer && for f in "src/app/(dashboard)/error.tsx" "src/app/(dashboard)/loading.tsx" "src/app/(portal)/error.tsx" "src/app/(portal)/loading.tsx"; do test -f "$f" && echo "EXISTS: $f" || (echo "MISSING: $f" && exit 1); done && grep -q "reset" "src/app/(dashboard)/error.tsx" && grep -q "reset" "src/app/(portal)/error.tsx" && grep -q "Loader2\|animate-spin" "src/app/(dashboard)/loading.tsx" && grep -q "Loader2\|animate-spin" "src/app/(portal)/loading.tsx" && echo "PASS: All error boundaries and loading states valid"</automated>
  </verify>
  <done>
- 4 files created: dashboard error + loading, portal error + loading
- All error boundaries are client components with reset button
- All loading states show a spinner with "Laden..." text
- Dashboard error links to /dashboard, portal error links to /portal
  </done>
</task>

</tasks>

<verification>
- 6 new files exist in correct locations
- `npx next build` still succeeds (error boundaries don't break build)
- All error.tsx files are client components
- All error.tsx files have reset functionality
</verification>

<success_criteria>
- Root error.tsx, not-found.tsx exist
- Dashboard error.tsx + loading.tsx exist
- Portal error.tsx + loading.tsx exist
- All in German, glass UI style, with recovery actions
</success_criteria>

<output>
After completion, create `.planning/phases/51-systematic-bug-audit-fix/51-03-SUMMARY.md`
</output>
