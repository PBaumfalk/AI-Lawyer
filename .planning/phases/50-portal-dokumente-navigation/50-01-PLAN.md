---
phase: 50-portal-dokumente-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(portal)/portal/akten/[id]/layout.tsx
  - src/app/(portal)/portal/akten/[id]/page.tsx
  - src/components/portal/portal-akte-tabs.tsx
autonomous: true
requirements: [DOC-02, DOC-03, DOC-04]
gap_closure: true

must_haves:
  truths:
    - "Mandant sieht auf der Akte-Detailseite eine Tab-Navigation mit Uebersicht, Dokumente, Nachrichten"
    - "Mandant kann durch Klick auf Dokumente-Tab zur Dokumentenliste navigieren"
    - "Mandant kann durch Klick auf Nachrichten-Tab zur Nachrichten-Seite navigieren"
    - "Aktive Tab ist visuell hervorgehoben (Underline)"
    - "Akte-Titel und Back-Link erscheinen oberhalb der Tabs und bleiben bei Tab-Wechsel stehen"
  artifacts:
    - path: "src/app/(portal)/portal/akten/[id]/layout.tsx"
      provides: "Shared layout with title + tabs for akte detail sub-pages"
      min_lines: 40
    - path: "src/components/portal/portal-akte-tabs.tsx"
      provides: "Tab bar component with active state detection"
      min_lines: 20
    - path: "src/app/(portal)/portal/akten/[id]/page.tsx"
      provides: "Uebersicht tab content (refactored, no title/back-link)"
      min_lines: 30
  key_links:
    - from: "src/app/(portal)/portal/akten/[id]/layout.tsx"
      to: "src/components/portal/portal-akte-tabs.tsx"
      via: "import and render"
      pattern: "PortalAkteTabs"
    - from: "src/components/portal/portal-akte-tabs.tsx"
      to: "/portal/akten/[id]/dokumente"
      via: "Next.js Link with usePathname active detection"
      pattern: "usePathname.*dokumente"
    - from: "src/app/(portal)/portal/akten/[id]/layout.tsx"
      to: "requireMandantAkteAccess"
      via: "server-side auth + access check"
      pattern: "requireMandantAkteAccess"
---

<objective>
Add tab-based sub-page navigation to the portal Akte detail page so Mandanten can discover and navigate to the Dokumente and Nachrichten pages via normal UI clicks.

Purpose: Close navigation gap -- DOC-02, DOC-03, DOC-04 pages exist but are unreachable without direct URL entry. Adding a tab bar makes them discoverable.
Output: layout.tsx with shared title/tabs, PortalAkteTabs client component, refactored page.tsx as Uebersicht tab content.
</objective>

<execution_context>
@/Users/patrickbaumfalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickbaumfalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-portal-dokumente-navigation/50-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/portal-access.ts:
```typescript
export async function getMandantAkten(userId: string): Promise<{ id: string; aktenzeichen: string; kurzrubrum: string; ... }[]>;
export async function requireMandantAkteAccess(akteId: string, userId: string): Promise<{ error?: string }>;
```

From current src/app/(portal)/portal/akten/[id]/page.tsx (to be refactored):
- Auth check: `session.user.role === "MANDANT"` with redirect to `/portal/login`
- Access check: `requireMandantAkteAccess(id, session.user.id)` with redirect to `/portal/dashboard`
- Prisma query for akte title data (aktenzeichen, kurzrubrum, beteiligte with GEGNER/GERICHT)
- Back link conditional on `getMandantAkten(session.user.id).length > 1`
- These concerns move UP to the new layout.tsx

From src/components/portal/portal-sidebar.tsx (pattern reference):
```typescript
// Active state pattern used in sidebar:
const isActive = pathname === item.href || pathname.startsWith(item.href + "/");
// Active classes: "bg-primary/10 text-primary font-medium"
// Inactive classes: "text-muted-foreground hover:text-foreground hover:bg-[var(--glass-card-bg)]"
```

Glass styling convention:
```
bg-white/50 dark:bg-white/[0.05] backdrop-blur-md rounded-xl border border-white/20 dark:border-white/[0.08]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create layout.tsx with shared title and PortalAkteTabs component</name>
  <files>src/app/(portal)/portal/akten/[id]/layout.tsx, src/components/portal/portal-akte-tabs.tsx</files>
  <action>
1. Create `src/components/portal/portal-akte-tabs.tsx` -- a client component ("use client"):
   - Props: `akteId: string`
   - Uses `usePathname()` from next/navigation to detect active tab
   - 3 tabs with equal width (flex, each flex-1):
     - Uebersicht: icon ClipboardList, href `/portal/akten/${akteId}`, active when pathname matches exactly
     - Dokumente: icon FileText, href `/portal/akten/${akteId}/dokumente`, active when pathname starts with href
     - Nachrichten: icon MessageSquare, href `/portal/akten/${akteId}/nachrichten`, active when pathname starts with href
   - Tab visual style per CONTEXT.md decisions:
     - Full-width container with subtle bottom border: `border-b border-[var(--glass-border-color)]`
     - Each tab is a Next.js Link with icon + text label, centered
     - Active tab: `text-primary font-medium` with a `border-b-2 border-primary` bottom border sitting on top of the container border
     - Inactive tab: `text-muted-foreground` with hover state `hover:text-foreground`
     - Smooth transition on color/border changes
   - Icons imported from lucide-react: ClipboardList, FileText, MessageSquare

2. Create `src/app/(portal)/portal/akten/[id]/layout.tsx` -- a server component:
   - Props: `{ children, params: Promise<{ id: string }> }`
   - Performs auth check (session.user.role === "MANDANT", redirect to /portal/login)
   - Calls `requireMandantAkteAccess(id, session.user.id)` -- redirect to /portal/dashboard on error
   - Fetches akte title data via Prisma (aktenzeichen, kurzrubrum only -- no beteiligte needed here)
   - Checks `getMandantAkten(session.user.id)` for multi-akte back link conditional
   - Renders:
     - Back link (conditional, same style as current page.tsx)
     - Title: h1 with aktenzeichen, p with kurzrubrum
     - `<PortalAkteTabs akteId={id} />` (the tab bar)
     - `{children}` below the tabs
   - Add `export const dynamic = "force-dynamic"`
  </action>
  <verify>
    <automated>cd /Users/patrickbaumfalk/Projekte/AI-Lawyer && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>layout.tsx renders shared title + tab navigation, PortalAkteTabs shows 3 tabs with active state detection via usePathname. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor page.tsx to be Uebersicht tab content only</name>
  <files>src/app/(portal)/portal/akten/[id]/page.tsx</files>
  <action>
Refactor the existing page.tsx to remove concerns that moved to layout.tsx:

1. Remove from page.tsx (now handled by layout):
   - Auth check (session check + redirect)
   - `requireMandantAkteAccess` call
   - Back link rendering
   - Page title (h1 aktenzeichen, p kurzrubrum)
   - `getMandantAkten` import and call

2. Keep in page.tsx (Uebersicht tab content):
   - Prisma query for akte detail data (beteiligte with GEGNER/GERICHT for AkteUebersicht component, naechsteSchritte for NaechsteSchritteCard, sachgebiet/status)
   - Gegner/Gericht name extraction logic
   - Two-column layout: SachstandTimeline (left 2/3) + AkteUebersicht + NaechsteSchritteCard (right 1/3)
   - `export const dynamic = "force-dynamic"`

3. The page still needs the akteId from params but does NOT need to re-check auth (layout does that). However since Next.js server components in the same route segment don't share request context, the page.tsx still needs to read params. Keep it as a server component with `params: Promise<{ id: string }>`.

4. IMPORTANT: The page.tsx should still have its own auth check for defense-in-depth. Keep the auth() call and MANDANT role check with redirect. This is a security best practice -- layout auth is the primary gate, page auth is the safety net. But remove the `requireMandantAkteAccess` call (layout handles it) and remove the `getMandantAkten` call (only needed for back link in layout).

5. Remove unused imports: ArrowLeft, Link (if no longer used), getMandantAkten.

6. The resulting page.tsx renders ONLY the two-column content area (timeline + sidebar cards) with no title, no back link, no tab bar. The surrounding `<div className="space-y-6">` wrapper stays for spacing of the content area.
  </action>
  <verify>
    <automated>cd /Users/patrickbaumfalk/Projekte/AI-Lawyer && npx tsc --noEmit --pretty 2>&1 | head -30 && echo "---BUILD---" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>page.tsx renders only Uebersicht content (timeline + cards). Title and tabs come from layout. Build succeeds. Navigating to /portal/akten/[id] shows title + tabs + overview content. Clicking Dokumente tab navigates to /portal/akten/[id]/dokumente. Clicking Nachrichten tab navigates to /portal/akten/[id]/nachrichten.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit` passes
2. Build succeeds: `npx next build` completes without errors
3. Route structure: layout.tsx wraps page.tsx, dokumente/page.tsx, and nachrichten/page.tsx
4. Tab navigation: PortalAkteTabs renders 3 tabs linking to correct URLs
5. Active state: Current pathname determines which tab gets primary underline
</verification>

<success_criteria>
- Akte detail page shows tab bar with Uebersicht | Dokumente | Nachrichten
- Clicking Dokumente tab navigates to /portal/akten/[id]/dokumente (existing page)
- Clicking Nachrichten tab navigates to /portal/akten/[id]/nachrichten (existing page)
- Title and back link persist across tab switches (rendered by layout)
- Active tab has primary-colored bottom border underline
- TypeScript and Next.js build pass
</success_criteria>

<output>
After completion, create `.planning/phases/50-portal-dokumente-navigation/50-01-SUMMARY.md`
</output>
