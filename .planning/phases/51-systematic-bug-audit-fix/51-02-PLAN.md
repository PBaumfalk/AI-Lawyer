---
phase: 51-systematic-bug-audit-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai/ollama.ts
  - src/lib/ai/provider.ts
  - src/lib/helena/complexity-classifier.ts
  - src/lib/settings/defaults.ts
  - .env
  - docker-compose.yml
  - .eslintrc.json
  - src/lib/finance/invoice/nummernkreis.ts
  - src/lib/helena/qa/retrieval-log.ts
  - src/lib/gamification/quest-evaluator.ts
  - src/lib/gamification/boss-engine.ts
  - src/lib/db.ts
  - src/components/ki/chat-layout.tsx
  - src/components/email/compose-popup.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "All files reference a single env var OLLAMA_URL for Ollama connection"
    - "ESLint runs without 'Definition for rule not found' errors"
    - "compose-popup.tsx auto-save uses current saveDraft reference"
  artifacts:
    - path: "src/lib/ai/ollama.ts"
      provides: "Ollama client using OLLAMA_URL"
      contains: "OLLAMA_URL"
    - path: "src/lib/ai/provider.ts"
      provides: "AI provider using OLLAMA_URL"
      contains: "OLLAMA_URL"
    - path: ".env"
      provides: "Single OLLAMA_URL env var"
      contains: "OLLAMA_URL"
    - path: ".eslintrc.json"
      provides: "Clean ESLint config"
  key_links:
    - from: "src/lib/ai/ollama.ts"
      to: ".env"
      via: "process.env.OLLAMA_URL"
      pattern: "process\\.env\\.OLLAMA_URL"
    - from: "src/lib/ai/provider.ts"
      to: ".env"
      via: "process.env.OLLAMA_URL"
      pattern: "process\\.env\\.OLLAMA_URL"
    - from: "src/lib/settings/defaults.ts"
      to: ".env"
      via: "process.env.OLLAMA_URL"
      pattern: "process\\.env\\.OLLAMA_URL"
---

<objective>
Standardize Ollama environment variable to OLLAMA_URL everywhere, clean up ESLint config, and fix useEffect dependency issues in compose-popup.tsx.

Purpose: The split OLLAMA_BASE_URL / OLLAMA_URL causes failures in both Docker and local dev (files using the wrong var fall back to hardcoded defaults that may be wrong for the environment). ESLint "rule not found" errors clutter the lint output. Stale closures in compose-popup auto-save can cause data loss.

Output: Consistent env vars, clean ESLint, fixed React dependency arrays.
</objective>

<execution_context>
@/Users/patrickbaumfalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickbaumfalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-systematic-bug-audit-fix/51-CONTEXT.md

@src/lib/ai/ollama.ts
@src/lib/ai/provider.ts
@src/lib/helena/complexity-classifier.ts
@src/lib/settings/defaults.ts
@src/lib/health/checks.ts
@src/lib/pii/ner-filter.ts
@src/lib/embedding/embedder.ts
@src/lib/ai/reranker.ts
@.eslintrc.json
@src/components/email/compose-popup.tsx
@.env
@docker-compose.yml

<interfaces>
<!-- Current Ollama env var usage across codebase -->

Files using OLLAMA_BASE_URL (need to change to OLLAMA_URL):
- src/lib/ai/ollama.ts:24 — `process.env.OLLAMA_BASE_URL ?? "http://localhost:11434"`
- src/lib/ai/provider.ts:74,169,210 — `process.env.OLLAMA_BASE_URL || "http://ollama:11434"`
- src/lib/helena/complexity-classifier.ts:259 — `process.env.OLLAMA_BASE_URL || "http://ollama:11434"`
- src/lib/settings/defaults.ts:127 — `process.env.OLLAMA_BASE_URL || "http://ollama:11434"`

Files already using OLLAMA_URL (correct, keep as-is):
- src/lib/health/checks.ts:19 — already correct
- src/lib/pii/ner-filter.ts:19 — already correct
- src/lib/embedding/embedder.ts:10 — already correct
- src/lib/ai/reranker.ts:7 — already correct

.env currently: OLLAMA_BASE_URL="http://localhost:11434"
docker-compose.yml: OLLAMA_URL: http://ollama:11434

Files with eslint-disable @typescript-eslint/no-explicit-any (8 comments to remove):
- src/lib/finance/invoice/nummernkreis.ts:28
- src/lib/helena/qa/retrieval-log.ts:38
- src/lib/gamification/quest-evaluator.ts:70,146,233
- src/lib/gamification/boss-engine.ts:375
- src/lib/db.ts:60
- src/components/ki/chat-layout.tsx:48
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize Ollama URL to single env var</name>
  <files>src/lib/ai/ollama.ts, src/lib/ai/provider.ts, src/lib/helena/complexity-classifier.ts, src/lib/settings/defaults.ts, .env, docker-compose.yml</files>
  <action>
Standardize all Ollama env var references to `OLLAMA_URL` with a single smart fallback: `http://localhost:11434`.

**4 files need OLLAMA_BASE_URL changed to OLLAMA_URL:**

1. `src/lib/ai/ollama.ts` line 23-24: Change `process.env.OLLAMA_BASE_URL` to `process.env.OLLAMA_URL`. Keep fallback as `http://localhost:11434`.

2. `src/lib/ai/provider.ts` lines 74, 169, 210: Change all 3 occurrences of `process.env.OLLAMA_BASE_URL || "http://ollama:11434"` to `process.env.OLLAMA_URL || "http://localhost:11434"`. The Docker environment explicitly sets OLLAMA_URL=http://ollama:11434, so the fallback should be for local dev (localhost).

3. `src/lib/helena/complexity-classifier.ts` line 259: Change `process.env.OLLAMA_BASE_URL || "http://ollama:11434"` to `process.env.OLLAMA_URL || "http://localhost:11434"`.

4. `src/lib/settings/defaults.ts` line 127: Change `process.env.OLLAMA_BASE_URL || "http://ollama:11434"` to `process.env.OLLAMA_URL || "http://localhost:11434"`.

**Environment files:**

5. `.env`: Change `OLLAMA_BASE_URL="http://localhost:11434"` to `OLLAMA_URL="http://localhost:11434"`. Keep OLLAMA_DEFAULT_MODEL unchanged.

6. `docker-compose.yml`: Already uses `OLLAMA_URL: http://ollama:11434` — no change needed. Verify both the `app` and `worker` services have it (lines 33 and 78).

**Do NOT touch files already using OLLAMA_URL** (checks.ts, ner-filter.ts, embedder.ts, reranker.ts) — they are correct.
  </action>
  <verify>
    <automated>cd /Users/patrickbaumfalk/Projekte/AI-Lawyer && echo "--- Checking no OLLAMA_BASE_URL remains in src/ ---" && ! grep -r "OLLAMA_BASE_URL" src/ && echo "PASS: No OLLAMA_BASE_URL in src/" && echo "--- Checking .env ---" && grep "OLLAMA_URL" .env && echo "--- Checking all OLLAMA_URL references ---" && grep -rn "OLLAMA_URL" src/lib/ai/ src/lib/helena/complexity-classifier.ts src/lib/settings/defaults.ts | head -20</automated>
  </verify>
  <done>
- Zero references to OLLAMA_BASE_URL in src/ directory
- .env uses OLLAMA_URL
- All 8 files consistently use process.env.OLLAMA_URL
- Fallback is http://localhost:11434 everywhere (Docker sets OLLAMA_URL explicitly)
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove invalid ESLint disable comments and fix useEffect deps</name>
  <files>.eslintrc.json, src/lib/finance/invoice/nummernkreis.ts, src/lib/helena/qa/retrieval-log.ts, src/lib/gamification/quest-evaluator.ts, src/lib/gamification/boss-engine.ts, src/lib/db.ts, src/components/ki/chat-layout.tsx, src/components/email/compose-popup.tsx</files>
  <action>
**ESLint cleanup:** The `.eslintrc.json` does not extend `@typescript-eslint`, so `@typescript-eslint/no-explicit-any` rule does not exist. Remove all 8 `eslint-disable-next-line @typescript-eslint/no-explicit-any` comments from:

1. `src/lib/finance/invoice/nummernkreis.ts:28` — remove the disable comment
2. `src/lib/helena/qa/retrieval-log.ts:38` — remove the disable comment
3. `src/lib/gamification/quest-evaluator.ts:70,146,233` — remove all 3 disable comments
4. `src/lib/gamification/boss-engine.ts:375` — remove the disable comment
5. `src/lib/db.ts:60` — remove the disable comment
6. `src/components/ki/chat-layout.tsx:48` — remove the disable comment

Do NOT add the `@typescript-eslint/eslint-plugin` — simpler to remove the comments since the rule is not enforced and `any` is used intentionally in these locations.

**compose-popup.tsx useEffect fixes:**

Line 153 (auto-save interval): The dependency array is `[dirty, kontoId, empfaenger, betreff]` but uses `saveDraft` which is defined elsewhere in the component. Add `saveDraft` to the dependency array. If `saveDraft` is not wrapped in useCallback, wrap it first, then add to deps.

Line 118 (email-konten fetch): The useEffect loads mailboxes and references `kontoId` (line 113: `if (list.length > 0 && !kontoId)`) but has empty dependency array `[]`. This is intentional (load once on mount) and acceptable — the `kontoId` reference is only used to avoid overwriting an existing selection. Leave as-is per the CONTEXT.md which marks this as LOW priority (issue 14). Only fix the auto-save dependency (issue 8, MEDIUM).
  </action>
  <verify>
    <automated>cd /Users/patrickbaumfalk/Projekte/AI-Lawyer && echo "--- ESLint disable comments check ---" && COUNT=$(grep -r "@typescript-eslint/no-explicit-any" src/ | wc -l | tr -d ' ') && echo "Remaining @typescript-eslint/no-explicit-any comments: $COUNT" && if [ "$COUNT" -eq "0" ]; then echo "PASS"; else echo "FAIL"; exit 1; fi && echo "--- ESLint run ---" && npx next lint 2>&1 | grep -c "Definition for rule" | xargs -I{} echo "Rule-not-found errors: {}"</automated>
  </verify>
  <done>
- Zero `@typescript-eslint/no-explicit-any` disable comments in codebase
- ESLint reports 0 "Definition for rule not found" errors
- compose-popup.tsx auto-save useEffect includes saveDraft in dependency array
  </done>
</task>

</tasks>

<verification>
- `grep -r "OLLAMA_BASE_URL" src/` returns nothing
- `npx next lint 2>&1 | grep "Definition for rule"` returns nothing
- `grep -r "@typescript-eslint/no-explicit-any" src/` returns nothing
</verification>

<success_criteria>
- Single env var OLLAMA_URL used across all 8 Ollama-referencing files
- Zero ESLint "rule not found" errors (was 7)
- compose-popup.tsx auto-save dependency array includes saveDraft
</success_criteria>

<output>
After completion, create `.planning/phases/51-systematic-bug-audit-fix/51-02-SUMMARY.md`
</output>
