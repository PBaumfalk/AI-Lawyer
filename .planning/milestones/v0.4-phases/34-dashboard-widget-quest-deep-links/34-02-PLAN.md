---
phase: 34-dashboard-widget-quest-deep-links
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/gamification/opt-in/route.ts
  - src/app/(dashboard)/einstellungen/page.tsx
  - src/components/kalender/kalender-liste.tsx
autonomous: true
requirements: [GAME-07]

must_haves:
  truths:
    - "User can toggle gamification opt-in on/off in the Einstellungen page under the Allgemein tab"
    - "Toggling opt-in on then navigating to dashboard shows the Quest Widget; toggling off hides it"
    - "Kalender page reads initial typ filter from URL search params so deep-links from quest widget pre-filter the view"
  artifacts:
    - path: "src/app/api/gamification/opt-in/route.ts"
      provides: "PATCH endpoint to toggle gamificationOptIn on User model"
      exports: ["PATCH"]
    - path: "src/app/(dashboard)/einstellungen/page.tsx"
      provides: "Gamification toggle switch in Allgemein tab"
      contains: "gamificationOptIn"
    - path: "src/components/kalender/kalender-liste.tsx"
      provides: "URL searchParams initialization for typ filter"
      contains: "useSearchParams"
  key_links:
    - from: "src/app/(dashboard)/einstellungen/page.tsx"
      to: "/api/gamification/opt-in"
      via: "fetch PATCH on toggle change"
      pattern: "fetch.*api/gamification/opt-in"
    - from: "src/components/kalender/kalender-liste.tsx"
      to: "URL searchParams"
      via: "useSearchParams for initial filter state"
      pattern: "useSearchParams"
---

<objective>
Add the gamification opt-in toggle to settings and wire deep-link target page filter initialization.

Purpose: Users control their gamification visibility from settings (GAME-07 opt-in requirement), and quest deep-links from Plan 01 actually pre-filter the target page view (completing the QUEST-08 deep-link experience).

Output: PATCH /api/gamification/opt-in endpoint, toggle switch in Einstellungen Allgemein tab, KalenderListe reads initial filter from URL params.
</objective>

<execution_context>
@/Users/patrickbaumfalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickbaumfalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing patterns the executor needs. -->

From src/app/(dashboard)/einstellungen/page.tsx:
- Client component ("use client")
- Session loaded via fetch("/api/auth/session") in useEffect
- Tabs component with "allgemein" as default tab
- Allgemein tab has two GlassCard sections: "Benutzer" (name/email/role) and "Verwaltung" (quick links)
- Gamification toggle should be added as a new GlassCard section inside the Allgemein tab, between Benutzer and Verwaltung

From src/components/ui/switch.tsx:
- Radix Switch component with brand-600 checked state
- Import: `import { Switch } from "@/components/ui/switch"`

From src/app/api/user/theme/route.ts (PATCH pattern for user preferences):
- Auth check, parse JSON body, update prisma.user, return updated value

From src/components/kalender/kalender-liste.tsx:
- Client component with `useState("")` for `typFilter`
- Filter buttons cycle through: all, TERMIN, FRIST, WIEDERVORLAGE
- Data fetched in useEffect dependent on `[typFilter, showErledigt]`
- Needs to read initial typFilter from URL searchParams on mount
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create opt-in PATCH endpoint + settings toggle UI</name>
  <files>src/app/api/gamification/opt-in/route.ts, src/app/(dashboard)/einstellungen/page.tsx</files>
  <action>
**1. Create PATCH /api/gamification/opt-in** at `src/app/api/gamification/opt-in/route.ts`:

- Auth check via `auth()` -- return 401 if no session
- Parse JSON body: `const { optIn } = await request.json()`
- Validate `optIn` is boolean -- return 400 if not
- Update user: `prisma.user.update({ where: { id: session.user.id }, data: { gamificationOptIn: optIn } })`
- If toggling ON and no GameProfile exists yet, call `getOrCreateGameProfile(session.user.id, session.user.role)` to auto-create it (so the widget works immediately)
- Return `{ gamificationOptIn: optIn }`

**2. Add gamification toggle to Einstellungen page** at `src/app/(dashboard)/einstellungen/page.tsx`:

- Add new state: `const [gamificationOptIn, setGamificationOptIn] = useState(false)` and `const [optInLoading, setOptInLoading] = useState(true)`
- In the existing session-loading `useEffect`, after session loads, fetch opt-in status:
  ```
  fetch("/api/gamification/opt-in").then(...) // We only have PATCH, so use a different approach:
  ```
  Actually, fetch the user's gamificationOptIn via a simple check: extend the existing session fetch or add a separate call to `/api/gamification/profile` and check if it returns 404 (opted out) or 200 (opted in). Simpler approach: add a GET handler to the opt-in route that returns `{ gamificationOptIn: boolean }`.

  **Revised approach for opt-in route**: Add both GET and PATCH handlers to `src/app/api/gamification/opt-in/route.ts`:
  - GET: query `prisma.user.findUnique({ where: { id }, select: { gamificationOptIn: true } })` and return `{ gamificationOptIn: boolean }`
  - PATCH: existing toggle logic

- After session loads, fetch `GET /api/gamification/opt-in` to get current opt-in state, set `gamificationOptIn` and `optInLoading = false`
- On toggle change, call `PATCH /api/gamification/opt-in` with `{ optIn: newValue }`, update local state on success, show `toast.success("Gamification " + (newValue ? "aktiviert" : "deaktiviert"))` on success, `toast.error` on failure

- **UI placement**: Inside `<TabsContent value="allgemein">`, add a new `<GlassCard>` section BETWEEN the existing "Benutzer" card and the "Verwaltung" card:
  ```
  <GlassCard className="p-6">
    <div className="flex items-center justify-between">
      <div>
        <h2 className="text-base font-semibold text-foreground">Gamification</h2>
        <p className="text-sm text-muted-foreground mt-1">
          Quest-Widget und XP-Fortschritt auf dem Dashboard anzeigen
        </p>
      </div>
      <Switch
        checked={gamificationOptIn}
        onCheckedChange={handleOptInToggle}
        disabled={optInLoading}
      />
    </div>
  </GlassCard>
  ```

- Import `Switch` from `@/components/ui/switch`
- Import `toast` from `sonner`
- The toggle handler: `async function handleOptInToggle(checked: boolean)` -- PATCH call, update state, toast feedback
  </action>
  <verify>
    <automated>cd /Users/patrickbaumfalk/Projekte/AI-Lawyer && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>GET /api/gamification/opt-in returns current opt-in status. PATCH toggles it. Einstellungen Allgemein tab shows a Gamification toggle card with Switch component. Toggling updates the DB and shows a toast confirmation.</done>
</task>

<task type="auto">
  <name>Task 2: Wire KalenderListe to read initial filter from URL search params</name>
  <files>src/components/kalender/kalender-liste.tsx</files>
  <action>
Modify `src/components/kalender/kalender-liste.tsx` to initialize `typFilter` from URL search params so that quest deep-links like `/kalender?typ=FRIST&datum=heute` pre-filter the view.

**Changes (minimal, ~5 lines added):**

1. Add import: `import { useSearchParams } from "next/navigation";`
2. Inside the component, call `const searchParams = useSearchParams();`
3. Change the initial `typFilter` state from `useState("")` to `useState(() => searchParams.get("typ") ?? "")` -- this reads the `typ` query param on mount and uses it as the initial filter value
4. The existing `useEffect` already re-fetches when `typFilter` changes, so the deep-link filter will automatically trigger the correct data fetch

**Important**: Do NOT modify the filter cycling behavior or the rest of the component. Only the initial state derivation changes. The `datum=heute` param from deep-links is informational context for the user (the Kalender already defaults to today's view) -- no additional handling needed.

**Why only KalenderListe**: The Tickets page already reads `searchParams` via its server component props. The Rechnungen page quest is "create invoices" where filtering is not meaningful. The Akten page quest is "update Akten" which navigates to the general Akten list. Only KalenderListe needs this adaptation per RESEARCH.md pitfall #2 analysis.
  </action>
  <verify>
    <automated>cd /Users/patrickbaumfalk/Projekte/AI-Lawyer && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Navigating to /kalender?typ=FRIST shows the Kalender pre-filtered to Frist entries. Deep-links from Quest Widget land on the correct filtered view.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `npx tsc --noEmit`
2. GET /api/gamification/opt-in returns `{ gamificationOptIn: true/false }`
3. PATCH /api/gamification/opt-in with `{ optIn: true }` updates the user record
4. Einstellungen > Allgemein tab shows Gamification toggle card
5. KalenderListe initialized with ?typ=FRIST shows only Frist entries on mount
</verification>

<success_criteria>
- User can toggle gamification on/off from Einstellungen Allgemein tab
- Toggle state persists across page navigations
- Quest deep-link /kalender?typ=FRIST correctly pre-filters KalenderListe
- No new npm packages installed
- Existing Einstellungen and Kalender functionality unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/34-dashboard-widget-quest-deep-links/34-02-SUMMARY.md`
</output>
