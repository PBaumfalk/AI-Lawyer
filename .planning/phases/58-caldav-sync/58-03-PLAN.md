---
phase: 58-caldav-sync
plan: 03
type: execute
wave: 3
depends_on: ["58-02"]
files_modified:
  - src/components/einstellungen/caldav-tab.tsx
  - src/app/(dashboard)/einstellungen/page.tsx
  - src/components/fristen/tagesuebersicht.tsx
  - src/app/api/kalender/route.ts
autonomous: true
requirements: [CAL-08]

must_haves:
  truths:
    - "User can manage CalDAV connections from a dedicated tab in Einstellungen"
    - "User can trigger manual sync and see sync status per connection"
    - "External calendar events from connected accounts are visible in the Kanzlei Tagesuebersicht alongside internal entries"
  artifacts:
    - path: "src/components/einstellungen/caldav-tab.tsx"
      provides: "Settings UI for CalDAV account management"
      min_lines: 80
    - path: "src/app/(dashboard)/einstellungen/page.tsx"
      provides: "CalDAV tab added to settings page"
      contains: "CalDavTab"
    - path: "src/components/fristen/tagesuebersicht.tsx"
      provides: "External events displayed alongside internal entries"
      contains: "EXTERN"
    - path: "src/app/api/kalender/route.ts"
      provides: "External events included in calendar API response"
      contains: "calDavSyncMapping"
  key_links:
    - from: "src/components/einstellungen/caldav-tab.tsx"
      to: "/api/caldav-konten"
      via: "fetch for CRUD and manual sync"
      pattern: "fetch.*api/caldav-konten"
    - from: "src/app/api/kalender/route.ts"
      to: "prisma.calDavSyncMapping"
      via: "query external events for Tagesuebersicht"
      pattern: "calDavSyncMapping"
    - from: "src/components/fristen/tagesuebersicht.tsx"
      to: "/api/kalender"
      via: "fetch includes external events"
      pattern: "typ.*EXTERN"
---

<objective>
Build the CalDAV settings UI tab for managing connections with manual sync, and integrate external calendar events into the Kanzlei Tagesuebersicht so users see a unified daily view.

Purpose: User-facing layer that makes CalDAV connections manageable and external events visible alongside internal Fristen/Termine.
Output: Settings tab with connection CRUD + sync controls, Tagesuebersicht showing external events.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-caldav-sync/58-01-SUMMARY.md
@.planning/phases/58-caldav-sync/58-02-SUMMARY.md

@src/components/einstellungen/caldav-tab.tsx (will create)
@src/app/(dashboard)/einstellungen/page.tsx (existing settings page to modify)
@src/components/fristen/tagesuebersicht.tsx (existing Tagesuebersicht to extend)
@src/app/api/kalender/route.ts (existing calendar API to extend)

<interfaces>
<!-- API endpoints from Plans 01-02 -->
```
GET  /api/caldav-konten          -> CalDavKonto[] (without secrets)
POST /api/caldav-konten          -> { konto, calendars: CalDavCalendarInfo[] }
PATCH /api/caldav-konten/[id]    -> CalDavKonto
DELETE /api/caldav-konten/[id]   -> 204
POST /api/caldav-konten/[id]/sync -> { message: "Synchronisation gestartet" }
```

<!-- Existing KalenderItem interface from tagesuebersicht.tsx -->
```typescript
interface KalenderItem {
  id: string;
  typ: "TERMIN" | "FRIST" | "WIEDERVORLAGE";
  titel: string;
  datum: string;
  datumBis: string | null;
  erledigt: boolean;
  prioritaet: string;
  istNotfrist: boolean;
  akteId: string | null;
  verantwortlichId: string;
  akte: { id: string; aktenzeichen: string; kurzrubrum: string } | null;
  verantwortlich: { id: string; name: string } | null;
}
```

<!-- Existing settings page tabs (einstellungen/page.tsx) -->
```
Tabs: VertretungUrlaubTab, ImportExportTab, FristenTab, VorlagenTab, BriefkopfTab,
      OrdnerSchemataTab, BenachrichtigungenTab, GamificationTab
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CalDAV settings tab + external events in Tagesuebersicht</name>
  <files>src/components/einstellungen/caldav-tab.tsx, src/app/(dashboard)/einstellungen/page.tsx, src/app/api/kalender/route.ts, src/components/fristen/tagesuebersicht.tsx</files>
  <action>
1. Create src/components/einstellungen/caldav-tab.tsx ("use client"):
- State: konten (CalDavKonto[]), loading, showAddDialog, selectedProvider ("GOOGLE"|"APPLE"|null)
- On mount: fetch GET /api/caldav-konten
- Render list of connected accounts as cards in GlassCard:
  - Each card: provider icon (Google=colored G icon, Apple=Apple icon from lucide), name, syncStatus badge (VERBUNDEN=green, FEHLER=red, GETRENNT=gray, SYNCHRONISIEREND=amber+spinner), letzterSync formatted
  - Actions: "Jetzt synchronisieren" button (POST /api/caldav-konten/[id]/sync, show toast "Synchronisation gestartet"), delete button (DELETE /api/caldav-konten/[id] with confirmation)
- Add connection dialog (triggered by "+ Kalender verbinden" button):
  - Step 1: Choose provider (two cards: Google Calendar, Apple iCloud Kalender)
  - Step 2 (Google): Input fields for name + OAuth2 instructions (show text: "Erstellen Sie ein App-Passwort oder verwenden Sie OAuth2 Token. Geben Sie Ihren Google-Account und das App-Passwort ein." -- NOTE: For v0.8, Google also uses app-password via CalDAV, not full OAuth2 flow which requires client_id registration. The serverUrl is pre-filled as https://apidata.googleusercontent.com/caldav/v2)
  - Step 2 (Apple): Input fields for name, Apple-ID (email), App-spezifisches Passwort. Pre-fill serverUrl as https://caldav.icloud.com. Show hint: "Erstellen Sie ein App-spezifisches Passwort unter appleid.apple.com > Sicherheit"
  - Step 3: POST to /api/caldav-konten, on success show returned calendars for selection (radio list), then PATCH selectedCalendarUrl
- Use shadcn/ui components: Button, GlassCard, Dialog (or custom modal like existing patterns), Input, Label
- Tailwind styling consistent with other settings tabs (BenachrichtigungenTab, GamificationTab for reference)

2. Add CalDAV tab to src/app/(dashboard)/einstellungen/page.tsx:
- Import CalDavTab from "@/components/einstellungen/caldav-tab"
- Add Calendar icon import from lucide-react (already imported in tagesuebersicht)
- Add new TabsTrigger "Kalender" and TabsContent with <CalDavTab /> to the tabs list
- Position after "Benachrichtigungen" tab

3. Extend src/app/api/kalender/route.ts GET handler:
- After fetching internal KalenderEintrag items, also query CalDavSyncMapping where richtung="PULL" AND konto.userId=session.user.id AND konto.aktiv=true
- For PULL mappings, query the associated external event data (stored in mapping or fetched from last sync cache)
- Actually: store external event summary data directly on CalDavSyncMapping model. Add fields to schema if not present, or use a Json field. SIMPLER APPROACH: Add `externalData Json?` field to CalDavSyncMapping in prisma/schema.prisma to cache { summary, description, dtstart, dtend, allDay }. Update sync-engine to populate this on pull.
- Map PULL events to KalenderItem format with typ="EXTERN" (we need to extend the KalenderTyp or use a virtual type)
- APPROACH: Do NOT change Prisma enum. Instead, in the API response, add external events as objects with a virtual typ "EXTERN" field. The Tagesuebersicht already filters by typ, so we add a new section.
- Query: `prisma.calDavSyncMapping.findMany({ where: { konto: { userId, aktiv: true }, richtung: "PULL", externalData: { not: null } }, include: { konto: { select: { name: true, provider: true } } } })`
- Filter by datum range (same as internal items), map to response format with id=mapping.id, typ="EXTERN", titel from externalData.summary, datum from externalData.dtstart, etc.
- Return combined array (internal items + external items)

4. Extend src/components/fristen/tagesuebersicht.tsx:
- Update KalenderItem interface: add typ option "EXTERN"
- Add state: externeTermine (KalenderItem[] filtered by typ="EXTERN")
- In fetchData, filter items for typ="EXTERN" into externeTermine state
- Add new section after Termine section: "Externe Kalender" with globe/calendar-sync icon
  - Each external item: colored dot (purple), titel, datum time, no erledigen button (read-only display)
  - Show provider info if available (from konto.name)
- Update totalItems to include externeTermine.length

5. Add externalData Json? field to CalDavSyncMapping in prisma/schema.prisma (if not already added in Plan 01). Run prisma db push.

6. Update src/lib/caldav/sync-engine.ts: When creating/updating PULL mappings, store { summary, description, dtstart, dtend, allDay } in externalData field.
  </action>
  <verify>
    <automated>cd /Users/jarvis/Projekte/AI-Lawyer && npx prisma db push --accept-data-loss 2>&1 | tail -3 && npx tsc --noEmit 2>&1 | grep -E "caldav|tagesuebersicht|einstellungen" | head -20</automated>
  </verify>
  <done>CalDAV tab visible in Einstellungen with connection management and manual sync, external calendar events appear in Tagesuebersicht under "Externe Kalender" section alongside internal Fristen/Termine/Wiedervorlagen</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all modified files
- Einstellungen page renders CalDAV tab without errors
- Tagesuebersicht renders external events section when PULL mappings exist
- GET /api/kalender returns both internal and external items
- Manual sync button triggers sync job and shows toast
</verification>

<success_criteria>
- CalDAV tab in Settings allows adding Google/Apple calendar connections
- Each connection shows sync status and last sync time
- Manual "Jetzt synchronisieren" button triggers immediate sync
- External calendar events visible in Tagesuebersicht with distinct "Externe Kalender" section
- Combined daily view shows Fristen + Wiedervorlagen + Termine + Externe Termine
</success_criteria>

<output>
After completion, create `.planning/phases/58-caldav-sync/58-03-SUMMARY.md`
</output>
