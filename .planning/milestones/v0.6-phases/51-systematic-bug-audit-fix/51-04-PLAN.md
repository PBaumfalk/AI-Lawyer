---
phase: 51-systematic-bug-audit-fix
plan: 04
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - package.json
  - src/lib/helena/__tests__/tools.test.ts
  - next.config.mjs
autonomous: true
requirements: []

must_haves:
  truths:
    - "npm test runs vitest and passes"
    - "create_draft_dokument test passes with correct assertion on result structure"
    - "TypeScript errors block the build (ignoreBuildErrors=false)"
  artifacts:
    - path: "package.json"
      provides: "test and test:watch scripts"
      contains: "vitest"
    - path: "src/lib/helena/__tests__/tools.test.ts"
      provides: "Fixed create_draft_dokument mock"
    - path: "next.config.mjs"
      provides: "Build config with TS error checking enabled"
      contains: "ignoreBuildErrors: false"
  key_links:
    - from: "package.json"
      to: "vitest.config.ts"
      via: "test script"
      pattern: "vitest"
    - from: "next.config.mjs"
      to: "tsconfig.json"
      via: "TypeScript checking during build"
      pattern: "ignoreBuildErrors"
---

<objective>
Add test scripts, fix broken test mock, and enable TypeScript build error checking.

Purpose: No `test` script in package.json causes confusion (devs might run `npx jest` instead of `npx vitest`). The create_draft_dokument test has a mock mismatch causing false failure. With TS errors fixed in Plan 01, we can now safely set `ignoreBuildErrors: false` so future TS errors are caught at build time.

Output: Working test script, passing test suite, build-time TS error checking.
</objective>

<execution_context>
@/Users/patrickbaumfalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickbaumfalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-systematic-bug-audit-fix/51-CONTEXT.md

@package.json
@src/lib/helena/__tests__/tools.test.ts
@src/lib/helena/tools/_write/create-draft-dokument.ts
@next.config.mjs

<interfaces>
<!-- Key context for the test fix -->

From src/lib/helena/tools/_write/create-draft-dokument.ts (the actual tool):
```typescript
// The tool's execute function returns:
return {
  data: {
    draftId: draft.id,  // "draft-1" from mock
    typ: "DOKUMENT",
    titel,
    status: "PENDING",
  },
  source: { table: "helena_drafts", id: draft.id },
};
```

The mock for helenaDraft.create returns:
```typescript
helenaDraft: {
  create: vi.fn(async (args: any) => ({
    id: "draft-1",
    ...args.data,
  })),
}
```

The tool also calls:
- `ctx.prisma.akte.findUnique()` — needs mock (currently only findFirst is mocked)
- `notifyDraftCreated()` — mocked elsewhere
- `createDraftActivity()` — mocked elsewhere

The test asserts `result.data.typ` and `result.data.status` — BUT the Vercel AI SDK `tool()` wrapper may transform the return value. The tool's execute returns `ToolResult` with `{ data: { draftId, typ, titel, status } }`. If the SDK wraps this, `result` may not have `.data` directly.

The test casts `as any` so it accesses result.data — need to verify what the actual return shape is. Most likely the mock for `akte.findUnique` is missing, causing an unhandled error that makes the result undefined.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test scripts and fix tools.test.ts mock</name>
  <files>package.json, src/lib/helena/__tests__/tools.test.ts</files>
  <action>
**package.json:** Add test scripts to the scripts section:
```json
"test": "vitest run",
"test:watch": "vitest"
```
Add them after the existing `dev:server` script. Do NOT change any other scripts.

**tools.test.ts (create_draft_dokument test, line ~426-453):** The test fails because `result.data` is undefined. The root cause is that the `create_draft_dokument` tool calls `ctx.prisma.akte.findUnique()` (line 58 in create-draft-dokument.ts) but the mock only has `akte.findFirst`. The missing `findUnique` mock causes an error that gets swallowed.

Fix the `createMockPrisma()` function (around line 113) to add `findUnique` to the akte mock:
```typescript
akte: {
  findFirst: vi.fn(async () => ({
    id: "akte-1",
    aktenzeichen: "123/26",
    // ... existing fields
  })),
  findUnique: vi.fn(async () => ({
    id: "akte-1",
    anwaltId: "user-1",
    sachbearbeiterId: null,
  })),
  // ... existing count, etc.
}
```

Also verify the test assertions match the actual return shape. The tool returns `{ data: { draftId, typ, titel, status }, source: {...} }`. The test asserts `result.data.typ` and `result.data.status` — these should work IF the mock is complete and the execute function runs to completion.

Additionally, mock `notifyDraftCreated` and `createDraftActivity` if not already mocked (check if they are mocked via vi.mock at the top of the file — they likely need mocks since they import from other modules).

Run the specific test to confirm: `npx vitest run src/lib/helena/__tests__/tools.test.ts`
  </action>
  <verify>
    <automated>cd /Users/patrickbaumfalk/Projekte/AI-Lawyer && npx vitest run src/lib/helena/__tests__/tools.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
- package.json has "test": "vitest run" and "test:watch": "vitest" scripts
- create_draft_dokument test passes (result.data is defined with correct shape)
- All other tests in tools.test.ts still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Enable TypeScript build error checking</name>
  <files>next.config.mjs</files>
  <action>
**Prerequisite:** This task depends on Plan 01 having fixed the TypeScript errors (falldaten-tab.tsx type mismatch, special-quests non-route export). With those fixed, `npx tsc --noEmit` should pass cleanly.

**next.config.mjs:** Change `ignoreBuildErrors: true` to `ignoreBuildErrors: false` in the typescript section:

```javascript
typescript: {
  ignoreBuildErrors: false,
},
```

Keep `eslint.ignoreDuringBuilds: true` unchanged — there are still 317 lint warnings and fixing those is out of scope for this phase.

After changing, verify with `npx next build` that the build succeeds without TS errors. If it fails on new TS errors not caught in the audit, document them but do NOT revert to `ignoreBuildErrors: true` — fix the errors instead.
  </action>
  <verify>
    <automated>cd /Users/patrickbaumfalk/Projekte/AI-Lawyer && grep -q "ignoreBuildErrors: false" next.config.mjs && echo "Config OK" && npx tsc --noEmit 2>&1 | tail -5 && echo "--- next build check ---" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
- next.config.mjs has ignoreBuildErrors: false
- npx tsc --noEmit passes with zero errors
- Future TypeScript errors will be caught at build time
  </done>
</task>

</tasks>

<verification>
- `npm test` runs vitest and reports all tests passing (416+ out of 427, excluding NER filter which needs Ollama)
- `npx tsc --noEmit` passes cleanly
- `next.config.mjs` has `ignoreBuildErrors: false`
</verification>

<success_criteria>
- "npm test" works (was missing)
- create_draft_dokument test passes (was failing)
- TypeScript errors block builds (was silently ignored)
- ESLint still runs with ignoreDuringBuilds: true (317 warnings, separate concern)
</success_criteria>

<output>
After completion, create `.planning/phases/51-systematic-bug-audit-fix/51-04-SUMMARY.md`
</output>
