---
phase: 63-tab-reduktion-keyfacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/akten/akte-detail-tabs.tsx
autonomous: true
requirements:
  - FEED-06

must_haves:
  truths:
    - "Only 4 primary tabs are visible in the tab bar: Aktivitaeten, Dokumente, Termine & Fristen, Finanzen"
    - "KI-Analyse and Falldaten are accessible via the overflow (three-dot) menu"
    - "The overflow button shows as active/highlighted when a hidden tab is currently selected"
    - "Switching to a hidden tab via the overflow menu works correctly and the correct content renders"
    - "Unsaved-changes guard for Falldaten still fires when switching away via the overflow menu"
  artifacts:
    - path: "src/components/akten/akte-detail-tabs.tsx"
      provides: "4-visible-tab layout with extended overflow menu"
      contains: "overflowTabs.*falldaten.*zusammenfassung"
  key_links:
    - from: "overflowTabs array"
      to: "handleTabChange"
      via: "button onClick in overflow dropdown"
      pattern: "handleTabChange.*value"
---

<objective>
Reduce the visible primary tab count from 6 to 4 by moving KI-Analyse and Falldaten into the existing overflow menu alongside Chat and Portal.

Purpose: The Akte detail page currently shows 6 tab triggers which crowds the tab bar. The requirement is 4-5 visible with the rest in an overflow menu.
Output: `akte-detail-tabs.tsx` with 4 primary tabs and 4 tabs in the overflow dropdown.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/akten/akte-detail-tabs.tsx
</context>

<interfaces>
<!-- Current tab structure in akte-detail-tabs.tsx -->
<!-- Primary TabsTrigger elements (currently 6): -->
<!--   feed (Aktivitaeten), dokumente, kalender, finanzen, falldaten, zusammenfassung -->
<!-- Current overflowTabs array (2 entries): -->
<!--   { value: "nachrichten", label: "Chat", icon: MessageSquare } -->
<!--   { value: "portal-nachrichten", label: "Portal", icon: UserCircle } -->
<!-- The overflow button highlights (overflowActive) when currentTab is in overflowTabs -->
<!-- handleTabChange respects the falldatenDirty guard -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Move KI-Analyse and Falldaten into the overflow menu</name>
  <files>src/components/akten/akte-detail-tabs.tsx</files>
  <action>
    In `akte-detail-tabs.tsx`, extend the `overflowTabs` array and remove two `TabsTrigger` elements:

    1. Add two entries to `overflowTabs` (before the existing Chat and Portal entries so secondary-level tabs are grouped logically):
       ```ts
       const overflowTabs = [
         { value: "falldaten", label: "Falldaten", icon: FileText },
         { value: "zusammenfassung", label: "KI-Analyse", icon: FileBarChart },
         { value: "nachrichten", label: "Chat", icon: MessageSquare },
         { value: "portal-nachrichten", label: "Portal", icon: UserCircle },
       ];
       ```
       `FileText` is already available via lucide-react imports (check existing imports — if not present, add it). `FileBarChart` and `MessageSquare` and `UserCircle` are already imported.

    2. Remove the two `TabsTrigger` elements for `falldaten` and `zusammenfassung` from the `TabsList`. The primary tab bar now contains only:
       - Aktivitaeten (feed)
       - Dokumente (dokumente) — keep the count badge
       - Termine & Fristen (kalender) — keep the count badge
       - Finanzen (finanzen)

    3. The `TabsContent` blocks for `falldaten` and `zusammenfassung` must remain unchanged — only the trigger is moving to the overflow, not the content panel.

    4. The `completeness` state, `falldatenDirty` guard, and `pendingTab` / `showUnsavedDialog` logic for Falldaten must remain intact. When Falldaten is selected via the overflow menu, `handleTabChange("falldaten")` is called — the existing guard fires correctly already.

    5. Do NOT rename, restyle, or restructure any other part of the component.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript compiles without errors; the `overflowTabs` array has 4 entries; the `TabsList` contains exactly 4 `TabsTrigger` elements.</done>
</task>

</tasks>

<verification>
After the change:
- `grep -c "TabsTrigger" src/components/akten/akte-detail-tabs.tsx` returns 4
- `grep "overflowTabs" src/components/akten/akte-detail-tabs.tsx` shows 4 entries
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
The Akte detail tab bar shows exactly 4 primary tabs. Falldaten and KI-Analyse appear in the three-dot overflow menu. Selecting either from the overflow renders the correct content. The unsaved-changes guard for Falldaten continues to work.
</success_criteria>

<output>
After completion, create `.planning/phases/63-tab-reduktion-keyfacts/63-01-SUMMARY.md`
</output>
