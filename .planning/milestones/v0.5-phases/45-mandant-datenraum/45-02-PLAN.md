---
phase: 45-mandant-datenraum
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - src/app/(portal)/dashboard/page.tsx
  - src/app/(portal)/akten/[id]/page.tsx
  - src/components/portal/akte-auswahl.tsx
  - src/components/portal/akte-uebersicht.tsx
  - src/components/portal/sachstand-timeline.tsx
  - src/components/portal/naechste-schritte-card.tsx
  - src/components/akten/naechste-schritte-editor.tsx
  - src/app/(dashboard)/akten/[id]/page.tsx
autonomous: true
requirements:
  - PORTAL-03
  - PORTAL-04
  - SACH-01
  - SACH-02
  - SACH-03

must_haves:
  truths:
    - "Mandant sees a dashboard listing only their own Akten with Aktenzeichen, Kurzrubrum, Sachgebiet, and Status"
    - "Mandant with multiple Akten can click to switch between them"
    - "Mandant with only one Akte is taken directly to the Akte detail view"
    - "Mandant sees a simplified timeline with key events (newest first) -- no internal Helena/email/notiz events visible"
    - "Mandant sees Akte overview with Sachgebiet, Gegner name, Gericht name, and Status"
    - "Mandant sees naechste Schritte prominently displayed on the Akte detail page"
    - "Anwalt can set naechste Schritte text from the internal Akte detail page"
  artifacts:
    - path: "src/app/(portal)/dashboard/page.tsx"
      provides: "Portal dashboard with Akte-Auswahl list or auto-redirect for single Akte"
      min_lines: 30
    - path: "src/app/(portal)/akten/[id]/page.tsx"
      provides: "Portal Akte detail page with Overview, Timeline, and Naechste Schritte"
      min_lines: 40
    - path: "src/components/portal/akte-auswahl.tsx"
      provides: "Akte selection card list for portal dashboard"
      min_lines: 25
    - path: "src/components/portal/akte-uebersicht.tsx"
      provides: "Akte overview component showing Sachgebiet, Gegner, Gericht, Status"
      min_lines: 30
    - path: "src/components/portal/sachstand-timeline.tsx"
      provides: "Simplified reverse-chronological timeline of mandantSichtbar events"
      min_lines: 40
    - path: "src/components/portal/naechste-schritte-card.tsx"
      provides: "Prominent card displaying the Anwalt-set naechste Schritte text"
      min_lines: 15
    - path: "src/components/akten/naechste-schritte-editor.tsx"
      provides: "Inline editor for Anwalt to set naechsteSchritte on internal Akte detail"
      min_lines: 30
  key_links:
    - from: "src/app/(portal)/dashboard/page.tsx"
      to: "/api/portal/akten"
      via: "fetch in server component or useEffect"
      pattern: "api/portal/akten"
    - from: "src/app/(portal)/akten/[id]/page.tsx"
      to: "/api/portal/akten/[id]"
      via: "fetch for Akte detail"
      pattern: "api/portal/akten.*id"
    - from: "src/components/portal/sachstand-timeline.tsx"
      to: "/api/portal/akten/[id]/timeline"
      via: "fetch with cursor pagination"
      pattern: "api/portal/akten.*timeline"
    - from: "src/components/akten/naechste-schritte-editor.tsx"
      to: "/api/akten/[id]/naechste-schritte"
      via: "PUT fetch on save"
      pattern: "api/akten.*naechste-schritte"
---

<objective>
Portal UI pages and Anwalt-side controls for the Mandant data room: dashboard with Akte selection, Akte detail page with overview/timeline/naechste-Schritte, and an internal editor for Anwalt to manage naechste Schritte text.

Purpose: Give Mandanten a clear, reassuring view of their case status with simplified timeline events and prominent next-steps guidance, while giving Anwaelte control over what information the Mandant sees.

Output: 2 portal pages (dashboard, akte detail), 4 portal components (akte-auswahl, akte-uebersicht, sachstand-timeline, naechste-schritte-card), 1 internal component (naechste-schritte-editor), updated internal Akte detail page.
</objective>

<execution_context>
@/Users/patrickbaumfalk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrickbaumfalk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-mandant-datenraum/45-CONTEXT.md
@.planning/phases/45-mandant-datenraum/45-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 45-01 that this plan implements against -->

From src/lib/portal-access.ts (created in 45-01):
```typescript
export async function getMandantAkten(userId: string): Promise<{
  id: string;
  aktenzeichen: string;
  kurzrubrum: string;
  sachgebiet: string;
  status: string;
  naechsteSchritte: string | null;
}[]>

export async function requireMandantAkteAccess(
  akteId: string,
  userId: string
): Promise<
  | { akte: any; error?: undefined }
  | { akte?: undefined; error: NextResponse }
>
```

API endpoints (created in 45-01):
- GET /api/portal/akten -> { akten: AkteSummary[] }
- GET /api/portal/akten/[id] -> { akte detail with sachgebiet, gegner, gericht, status, naechsteSchritte }
- GET /api/portal/akten/[id]/timeline -> { items: TimelineEvent[], nextCursor, hasMore }
- PUT /api/akten/[id]/naechste-schritte -> { naechsteSchritte }

Existing patterns:
```typescript
// Glass UI tokens from globals.css:
// glass-card, glass-panel, glass-sidebar, glass-input
// oklch color system with blur tiers

// Portal layout (from Phase 43): src/app/(portal)/layout.tsx
// Already has SessionProvider, sidebar, header for MANDANT role
// Uses same Glass look as internal app
```

Existing AktenActivityTyp values and their mandantSichtbar defaults (from 45-CONTEXT.md):
- DOKUMENT: true (if document freigegeben)
- STATUS_CHANGE: true
- HELENA_DRAFT: false
- HELENA_ALERT: false
- EMAIL: false
- NOTIZ: false
- BETEILIGTE: false
- FRIST: false (by default)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Portal dashboard page with Akte-Auswahl</name>
  <files>src/app/(portal)/dashboard/page.tsx, src/components/portal/akte-auswahl.tsx</files>
  <action>
  **1. Portal dashboard page (`src/app/(portal)/dashboard/page.tsx`):**
  - Server component with `export const dynamic = "force-dynamic"`
  - Call `auth()` to get session, redirect to /portal/login if not authenticated
  - Fetch Mandant's Akten via direct Prisma query using `getMandantAkten(session.user.id)` imported from portal-access.ts (server component can call directly, no API fetch needed)
  - If Mandant has exactly 1 Akte: `redirect(\`/portal/akten/\${akten[0].id}\`)` -- skip the selection screen
  - If Mandant has 0 Akten: show an empty state message ("Sie haben derzeit keine aktiven Vorgaenge. Bitte wenden Sie sich an Ihre Kanzlei.")
  - If Mandant has 2+ Akten: render the `<AkteAuswahl>` component with the list
  - Page title: "Meine Vorgaenge" (avoid legal jargon -- "Vorgaenge" is more approachable than "Akten" for Mandanten)

  **2. Akte-Auswahl component (`src/components/portal/akte-auswahl.tsx`):**
  - Client component ("use client")
  - Receives `akten` array as prop
  - Renders a responsive grid of glass-card styled cards (1 col mobile, 2 cols md+)
  - Each card shows:
    - Aktenzeichen as title
    - Kurzrubrum as subtitle
    - Sachgebiet as a subtle badge (use SACHGEBIET_LABELS mapping for German display names, e.g., ARBEITSRECHT -> "Arbeitsrecht")
    - AkteStatus as a colored badge (OFFEN=emerald, RUHEND=amber, ARCHIVIERT=gray, GESCHLOSSEN=gray)
    - naechsteSchritte preview (first ~80 chars if set, or "Keine Informationen" in muted text)
  - Each card links to `/portal/akten/[id]` using Next.js `<Link>`
  - Hover effect: subtle border glow or scale (consistent with Glass UI)
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
  - Portal dashboard renders Akte list for multi-Akte Mandanten (PORTAL-04)
  - Single-Akte Mandanten auto-redirect to detail page
  - Zero-Akte Mandanten see friendly empty state
  - Cards use Glass UI styling with oklch tokens
  - Each card links to portal Akte detail page
  </done>
</task>

<task type="auto">
  <name>Task 2: Portal Akte detail page with Overview, Timeline, Naechste Schritte + Anwalt editor</name>
  <files>src/app/(portal)/akten/[id]/page.tsx, src/components/portal/akte-uebersicht.tsx, src/components/portal/sachstand-timeline.tsx, src/components/portal/naechste-schritte-card.tsx, src/components/akten/naechste-schritte-editor.tsx, src/app/(dashboard)/akten/[id]/page.tsx</files>
  <action>
  **1. Portal Akte detail page (`src/app/(portal)/akten/[id]/page.tsx`):**
  - Server component with `export const dynamic = "force-dynamic"`
  - Call `auth()`, verify MANDANT role, redirect to /portal/login if not authenticated
  - Fetch Akte detail from GET /api/portal/akten/[id] (or call requireMandantAkteAccess + direct Prisma query)
  - If akte not found/unauthorized: redirect to /portal/dashboard
  - Layout structure:
    - Back link to /portal/dashboard (only if Mandant has multiple Akten -- pass count from server)
    - Page title: Aktenzeichen + Kurzrubrum
    - Two-column layout (lg+ breakpoint): left column 2/3 width for Timeline, right column 1/3 for Overview + Naechste Schritte
    - On mobile: single column, Overview + Naechste Schritte first, then Timeline below
  - Render three components: `<AkteUebersicht>`, `<NaechsteSchritteCard>`, `<SachstandTimeline>`

  **2. AkteUebersicht component (`src/components/portal/akte-uebersicht.tsx`):**
  - Server or client component (stateless display)
  - Receives akte data as props: sachgebiet, status, gegnerName, gerichtName, kurzrubrum
  - Renders a glass-card with a clean info grid:
    - "Sachgebiet:" + human-readable label (e.g., "Arbeitsrecht")
    - "Status:" + colored badge (same colors as Akte-Auswahl)
    - "Gegner:" + name (or "Nicht angegeben" if null)
    - "Gericht:" + name (or "Nicht angegeben" if null)
  - Use subtle dividers between rows
  - Card header: "Uebersicht" with a subtle icon

  **3. NaechsteSchritteCard component (`src/components/portal/naechste-schritte-card.tsx`):**
  - Receives `naechsteSchritte: string | null` as prop
  - Renders a glass-card with prominent styling:
    - Card header: "Naechste Schritte" with an arrow/forward icon
    - If text exists: render the text in a larger, readable font
    - If text is null/empty: show "Ihr Anwalt hat noch keine naechsten Schritte hinterlegt." in muted text
  - This should be the MOST prominent card on the page -- slightly larger, maybe a subtle accent border (per CONTEXT: "#1 thing a Mandant wants to know is what happens next")

  **4. SachstandTimeline component (`src/components/portal/sachstand-timeline.tsx`):**
  - Client component ("use client") for pagination
  - Fetches from GET /api/portal/akten/[id]/timeline with cursor pagination
  - Renders a vertical timeline with events in reverse chronological order
  - Each timeline event shows:
    - Date (formatted as "DD.MM.YYYY" German format)
    - Type icon (use simple icons per AktenActivityTyp: document icon for DOKUMENT, calendar for FRIST, arrow for STATUS_CHANGE, etc.)
    - Title (from activity.titel)
    - Optional short description (from activity.inhalt, truncated)
  - "Mehr laden" button at bottom if hasMore=true (loads next page via cursor)
  - Empty state: "Noch keine Eintraege vorhanden." in muted text
  - Style: clean list with subtle left border/line connecting events (timeline visual), glass-card container
  - German date formatting: use `new Intl.DateTimeFormat("de-DE", { day: "2-digit", month: "2-digit", year: "numeric" })` or similar

  **5. NaechsteSchritteEditor component (`src/components/akten/naechste-schritte-editor.tsx`):**
  - Client component ("use client") for the INTERNAL Akte detail page (Anwalt side)
  - Receives `akteId: string` and `initialText: string | null` as props
  - Renders a collapsible section or inline editable area:
    - Label: "Naechste Schritte (fuer Mandant)"
    - Textarea for editing the text (glass-input styling)
    - "Speichern" button that calls PUT /api/akten/[id]/naechste-schritte
    - Show toast on success ("Naechste Schritte gespeichert") via sonner
    - Show toast on error ("Fehler beim Speichern")
  - Subtle helper text: "Dieser Text ist fuer den Mandanten im Portal sichtbar."

  **6. Wire NaechsteSchritteEditor into internal Akte detail page:**
  - Edit `src/app/(dashboard)/akten/[id]/page.tsx`
  - Add the `<NaechsteSchritteEditor>` component in an appropriate location (e.g., after the case info section, before the activity feed)
  - Pass `akteId` and `naechsteSchritte` from the Akte data
  - Only render for users with role ANWALT, SACHBEARBEITER, or ADMIN (not SEKRETARIAT -- this is a substantive legal communication)
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
  - Portal dashboard shows Akte list or auto-redirects for single Akte (PORTAL-04)
  - Portal Akte detail page shows Overview, Naechste Schritte, and Timeline (SACH-01, SACH-02, SACH-03)
  - Timeline only shows mandantSichtbar=true events with no internal data leaking (PORTAL-03)
  - NaechsteSchritteCard is the most prominent element on the page
  - Anwalt can edit naechsteSchritte from internal Akte detail page (SACH-02)
  - All components use Glass UI styling (glass-card, glass-panel tokens)
  - German date formatting and labels throughout
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit` passes
2. Portal dashboard page exists at src/app/(portal)/dashboard/page.tsx
3. Portal Akte detail page exists at src/app/(portal)/akten/[id]/page.tsx
4. All 4 portal components exist in src/components/portal/
5. NaechsteSchritteEditor component exists in src/components/akten/
6. Internal Akte detail page includes NaechsteSchritteEditor
7. Timeline fetches from /api/portal/akten/[id]/timeline (grep confirms URL)
8. No internal data fields (anwaltId, meta, userId) appear in portal components
</verification>

<success_criteria>
- Mandant dashboard shows only their Akten with Akte-Auswahl (PORTAL-03, PORTAL-04)
- Single-Akte Mandant auto-redirects to detail
- Akte detail shows Overview card with Sachgebiet, Gegner, Gericht, Status (SACH-03)
- Naechste Schritte card is prominently displayed (SACH-02)
- Sachstand timeline shows only mandantSichtbar events in reverse chronological order (SACH-01)
- Anwalt can set naechsteSchritte from internal dashboard (SACH-02)
- All UI uses Glass UI tokens and German labels
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/45-mandant-datenraum/45-02-SUMMARY.md`
</output>
